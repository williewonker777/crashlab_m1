# 강의 ② 챕터 3 — 차동 구동과 로봇 상태 표현

챕터 표지는 24페이지, 본문은 25~44페이지다. 차동 구동 기구학으로 바퀴와 로봇의 움직임을 연결한 뒤, 그 결과를 odom·frame·TF·quaternion으로 표현한다. 챕터 4는 45페이지부터 이어진다.

## 강의 흐름

| 페이지 | 내용 |
| --- | --- |
| 25~27 | 차동 구동 모델과 가정, 바퀴 각속도·선속도, 직진·선회·제자리 회전 |
| 28~30 | 정기구학, 순간 회전 중심 ICC, 역기구학과 속도 명령 |
| 31~33 | 공간 좌표의 속도, 엔코더 이동량, 미드포인트 적분 |
| 34 | 좌우 바퀴 속도를 바꾸는 주행 궤적 시연 |
| 35~37 | 오도메트리와 드리프트, 프레임, map·odom·base_link |
| 38~39 | TF 트리와 시간, 센서 좌표를 odom으로 바꾸는 계산 예시 |
| 40~41 | RPY와 quaternion, yaw–quaternion 변환 시연 |
| 42~44 | Odometry 메시지, odom과 TF의 관계, 명령부터 상태 추정까지의 흐름 |

## 원자료에서 차용한 부분

로컬 원자료: `~/Downloads/2025 모바일로봇 제어.pptx` (33장). 슬라이드의 수식과 예시를 HTML·SVG로 다시 구성했으며, 원본 PPT 파일은 저장소에 복사하지 않는다.

| 원자료 페이지 | 반영 내용 |
| --- | --- |
| 21~22 | Differential Drive Kinematics: 좌우 바퀴 속도와 로봇의 전진·회전 속도, ICC, 정·역기구학 |
| 26 | 중간 방향 `θ + Δθ/2`를 이용한 위치 누적 |
| 27~28 | odom·frame·TF, 센서 전방 0.3 m 장착과 몸체 위치 `(2, 1)`, yaw 90° 예시 |
| 29~30 | Odometry에 위치와 quaternion을 담는 관계, RPY와 quaternion |

예제에서 가정한 로봇 크기는 모든 본문과 시연에서 **r = 0.10 m, L = 0.50 m**로 통일했다. 실제 M1 사양을 뜻하지 않는다. `/cmd_vel`의 `geometry_msgs/msg/Twist` 표기는 원자료의 인터페이스 예시다. 실제 노드의 토픽·메시지 형식은 해당 구성에서 확인해야 한다.

## 25페이지 — 예제 로봇의 크기를 먼저 정의

‘계산용 치수’라는 이름과 기호만 제시하지 않고, 이후 계산·주행 시연에 공통으로 쓰는
가상 로봇의 크기라는 점을 명시한다. 단위는 이후 계산과 같은 m로 통일하고,
왼쪽 표에 r = 0.10 m, L = 0.50 m를 한 번씩 표시한다.

- 개별 환산 설명은 뒤의 기구학 페이지에서 다룬다. 25페이지는 바퀴 크기와 간격으로
  로봇 이동을 계산한다는 핵심 문장만 둔다.
- 실제 앨리스 M1 이동부 사진(`assets/img/m1-3q.png`)을 크게 배치하고, 옆에
  위에서 본 차동 구동 모델과 옆에서 본 바퀴 도식을 둔다. 원본 사진을 SVG의
  viewBox로 이동부만 확대해 보여준다.
- 사진에서 가려진 바퀴 중심을 임의로 측정하지 않는다. 개념도에 바퀴 중심 두 곳과
  L의 치수선을 맞추고, r은 바퀴 중심부터 테두리까지 표시한다.
- 사진은 실제 로봇을 보여주는 예시이고, 도식과 표의 r·L은 강의용 모델이다.
  그림에는 기호와 짧은 이름만 두며, 실제 M1 사양이 아닌 예제용 가정임을 한 줄로 표시한다.
- 두 구동륜과 보조 바퀴, 평평한 바닥에서 미끄러짐 없이 굴러가는 모델이라는
  조건을 유지한다. 좌표축 상세 대신 두 길이의 측정 위치를 중심으로 그림을 정리한다.

## 26페이지 — 속도 변환과 측정값의 기준

- 좌우 계산을 두 줄로 나눠 각속도와 곱셈 과정을 보여준다. 반지름 0.10 m에
  왼쪽 4 rad/s, 오른쪽 6 rad/s를 곱하면 각각 0.4 m/s, 0.6 m/s다.
- rpm은 1분 동안 회전하는 횟수다. 한 회전은 2π rad, 한 분은 60초이므로
  rpm 값에 2π/60을 곱해 rad/s로 바꾼다.
- 모터 축에서 측정한 각속도는 감속비로 나눠 바퀴 각속도로 환산한다.
  여기서 감속비는 모터 회전수/바퀴 회전수이며, 10:1이면 바퀴 각속도는 모터의 1/10이다.
  모터와 바퀴의 회전 방향 부호는 동일한 전진 기준으로 맞춘 뒤 사용한다.
- 이 페이지의 vL·vR은 각 바퀴의 선속도다. 로봇 중심의 전진 속도는 두 값의
  평균으로 구하며 뒤의 정기구학 페이지에서 다룬다.

## 27페이지 — 바퀴 속도 조합에서 로봇의 움직임으로

- 직진·왼쪽 선회·반시계 제자리 회전을 짧은 문장과 경로 그림으로 비교한다.
  그림은 위에서 본 모습이며, 위쪽이 로봇의 현재 전방이다.
- 남색 화살표는 좌우 바퀴 속도다. 직진에서는 같은 길이로 앞으로, 왼쪽 선회에서는
  오른쪽이 더 길게 앞으로, 제자리 회전에서는 같은 길이로 반대 방향을 가리킨다.
- 청록색은 로봇의 이동·회전이다. 직선과 왼쪽 원호는 중심의 이동 경로이고,
  제자리 회전의 원형 화살표는 고정된 중심 주위로 몸체 방향이 바뀌는 것을 뜻한다.
  도식의 경로 길이·반지름은 계산값에 맞춘 축척이 아닌 움직임을 보여주는 개념도다.
- 카드마다 바퀴 조건과 로봇 결과를 구분한다. 직진은 `vL = vR > 0`,
  왼쪽 선회는 `vR > vL > 0`, 그림의 반시계 제자리 회전은 `vR = −vL > 0`이다.
  세 예시 모두 바퀴가 정지한 경우를 제외한다. 두 바퀴 속도가 모두 0이면 정지다.
- 앞 페이지의 `ωL·ωR`은 바퀴 각속도지만, 여기의 `ω`는 로봇 몸체의 회전 속도다.
  `vL·vR`은 바퀴 선속도, `v`는 로봇 중심의 이동 속도로 하단에 정의한다.
  위에서 볼 때 반시계 방향이 양의 `ω`다.

## 28페이지 — 계산 조건과 결과를 연결

제목 아래에서 이 차동 구동 예제의 정기구학을 “바퀴의 속도를 알 때 로봇의 이동·회전 속도를 구하는 것”으로 짧게 정의한다.

왼쪽 바퀴 0.4 m/s, 오른쪽 바퀴 0.6 m/s, 바퀴 중심 간격 L = 0.50 m를
계산 앞에 함께 표시한다. v는 로봇 중심의 이동 속도, ω는 로봇 몸체의 회전 속도로
설명하고, 두 결과를 “0.5 m/s로 이동하면서 왼쪽으로 0.4 rad/s로 회전한다”로 연결한다.
회전 식의 분모는 위에 표시한 바퀴 중심 간격이다. 같은 속도 차이라면 간격이 클수록
천천히 회전한다는 관계도 유지한다. 왼쪽으로 회전한다는 해석은 ω > 0인 이 예제에 해당한다.

## 29페이지 — 순간 회전 중심과 바퀴별 반경

- ICC(Instantaneous Center of Curvature)는 현재의 움직임을 원운동으로 볼 때의 중심이다.
  영문 약자 풀이는 페이지 상단에 표시한다.
  현재 바퀴 속도가 바뀌면 ICC도 달라질 수 있으며, 그림은 미래 궤적을 확정하는 예측이 아니다.
- 배울 목적을 ‘원하는 곡선으로 주행하기’로 먼저 제시한다. R = v/ω는 현재 움직임의
  회전 반경을 해석할 때, ω = v/R는 원하는 반경에 맞는 회전 속도를 정할 때 사용한다.
  ICC는 회전을 이해하는 기하학적 기준점이며, 바퀴 명령을 만들 때 매번 ICC 좌표를
  계산해야 하는 것은 아니다. R이 주행 곡선과 속도 명령을 연결한다는 점을 설명한다.
- 적용 예제는 ‘반경 1.25 m인 왼쪽 곡선을 v = 0.5 m/s로 주행’이다.
  ω = 0.5/1.25 = 0.4 rad/s를 정한 뒤 다음 30페이지에서 역기구학으로 바퀴 명령을 구한다.
  r = 0.10 m, L = 0.50 m일 때 왼쪽 4 rad/s, 오른쪽 6 rad/s로 이어진다.
  v와 ω를 일정하게 유지하면 이 반경의 원호를 따라간다는 이상적인 미끄럼 없는 모델이다.
- R은 ICC에서 로봇 중심까지의 거리다. 일반적인 부호 있는 R = v/ω와 달리
  이 좌회전 예제에서는 R > 0이므로 거리로 읽는다. ω = v/R로 곡선 주행 명령을
  정하는 경우 R ≠ 0을 전제로 하며, 제자리 회전은 별도 조건으로 표시한다.
- L은 좌우 바퀴 중심 간격이다. 왼쪽 바퀴는 로봇 중심보다 ICC에 L/2만큼 가깝고,
  오른쪽 바퀴는 L/2만큼 멀다. 세 동심 원호와 중심에 맞춘 치수선으로
  안쪽 R − L/2, 중심 R, 바깥쪽 R + L/2를 연결한다.
- 두 바퀴는 같은 시간에 같은 각도만큼 ICC 주위를 돈다. 원호가 긴 바깥쪽 바퀴가
  더 빠르며 vL = ω(R − L/2), vR = ω(R + L/2)다. 여기의 ω는 로봇 몸체의 회전 속도다.
- SVG의 ICC는 (90, 260), 바퀴·몸체 중심은 x = 410, 490, 570이다.
  반경 320·400·480은 예제의 1.00·1.25·1.50 m와 같은 비율이고,
  세 원호는 같은 30° 범위를 사용한다. 남색 바퀴 속도 화살표의 길이도 2:3으로 맞춘다.
  몸체는 x = 420~560으로 그려 양쪽 바퀴 안쪽과 겹쳐 연결되게 한다. 바퀴 중심·반경은 유지한다.
- 직진은 v ≠ 0, ω = 0일 때 유한한 회전 중심이 없다. 제자리 회전은
  v = 0, ω ≠ 0일 때 R = 0이다. 완전 정지(v = ω = 0)는 R = v/ω로 정의하지 않는다.

## 30페이지 — 로봇 속도 목표에서 바퀴 명령으로

- 역기구학은 원하는 로봇의 이동·회전 속도로 각 바퀴의 각속도 명령을 구하는 과정이다.
  29페이지의 곡선 주행에서 정한 v = 0.5 m/s, ω = 0.4 rad/s를 그대로 입력으로 쓴다.
- 회전 때문에 좌우 선속도에 빼고 더할 값은 Lω/2 = 0.10 m/s다.
  왼쪽은 vL = 0.5 − 0.10 = 0.4 m/s, 오른쪽은 vR = 0.5 + 0.10 = 0.6 m/s다.
- 그다음 바퀴 반지름 r = 0.10 m로 나누어 ωL = 4 rad/s, ωR = 6 rad/s를 구한다.
  앞의 0.10 m/s와 분모의 0.10 m가 서로 다른 물리량임을 두 단계와 단위로 구분한다.
- 원자료의 `/cmd_vel`·Twist 예시에서 `linear.x`는 몸체 전진 속도 v,
  `angular.z`는 몸체 회전 속도 ω에 대응한다. 나머지 성분은 0이다.
  이 메시지는 로봇 몸체의 속도 목표이며, 바퀴 각속도 목표는 역기구학을 거쳐 계산한다.
  실제 구성의 토픽·메시지 형식은 별도로 확인한다.
- 여기서 구한 값은 바퀴 축 기준의 목표다. 모터 축 명령으로 바꿀 때의 감속비와 부호,
  실제 속도를 목표에 맞추는 피드백 제어는 앞의 모터·오차 제어 내용과 연결한다.

## 31페이지 — 몸체 속도를 바닥 좌표의 변화로 연결

- 바닥에 고정된 x·y축에서 로봇 중심의 위치를 (x, y)로 읽는다. θ는 고정된 +x축에서
  로봇 전방까지 반시계 방향으로 잰 각도다. 로봇이 돌면 θ가 바뀌고 바닥 축은 그대로다.
- ẋ = v cos θ, ẏ = v sin θ, θ̇ = ω를 각각 x 방향 속도, y 방향 속도,
  방향이 변하는 속도로 설명한다. 기호 위의 점은 시간에 따른 변화율이며
  ẋ·ẏ의 단위는 m/s, θ̇·ω의 단위는 rad/s다.
- 그림과 계산은 현재 θ = 45°, v = 0.5 m/s인 순간으로 통일한다.
  ẋ = ẏ ≈ 0.35 m/s이며 두 성분의 벡터 크기가 v다. 두 값을 단순히 더하지 않는다.
  몸체가 회전 중이라면 이후 θ와 두 성분도 바뀌므로, 이 방향으로 계속 직진한다는 예측은 아니다.
- 몸체의 옆 방향 속도가 0이라는 조건을 모호한 vy 표기로 다시 제시하지 않고,
  로봇이 자기 앞쪽으로 전진해도 바닥 기준의 x·y는 함께 증가할 수 있다는 문장으로 설명한다.
- 짧은 시간의 위치 변화는 그 구간의 속도에 시간을 곱해 근사할 수 있다.
  측정한 바퀴 이동량을 바닥 좌표로 바꿔 누적하는 과정이 오도메트리의 위치·방향 추정으로
  이어진다는 목적을 하단에 표시한다. 다음 페이지에서 엔코더 이동량을 읽고 적분으로 연결한다.
  실제 추정은 측정값을 사용하며, 명령 속도만으로 실제 위치를 확정하지 않는다.

## 32페이지 — 엔코더의 변화량에서 이동·회전량으로

- Δ는 이번 측정값에서 이전 측정값을 뺀 변화량이다. 좌우 바퀴는 같은 시작·종료 시각의
  측정 구간을 사용한다. Δφ는 엔코더 측정값을 바퀴 축 각도(rad)로 환산한 뒤 얻은 회전량이다.
  모터 축 엔코더의 감속비와 전진 방향 부호는 앞의 속도 환산 기준을 그대로 반영한다.
- r = 0.10 m, L = 0.50 m를 계산 앞에 함께 제시한다. ΔφL = 0.4 rad와
  ΔφR = 0.6 rad에서 ΔsL = 0.10 × 0.4 = 0.04 m,
  ΔsR = 0.10 × 0.6 = 0.06 m로 바뀌는 과정을 왼쪽에 표시한다.
- 바퀴 이동량의 평균은 로봇 중심의 진행 경로를 따른 이동량 Δs = 0.05 m다.
  차이를 바퀴 중심 간격 L로 나눈 값은 몸체의 방향 변화량 Δθ = 0.04 rad다.
  위에서 볼 때 반시계(왼쪽) 회전을 양수로 읽는다. Δφ는 바퀴 회전량이고 Δθ는 몸체 회전량이다.
- 슬라이드의 ‘진행 경로를 따라 간 길이’는 전진하는 이 예제의 Δs > 0에 해당한다.
  일반식에서 Δs는 부호 있는 이동량으로 후진하면 음수이며, 임의의 고정 x축 변위와는 다르다.
  미끄러짐 없는 차동 구동 모델의 바퀴·몸체 관계를 사용한다.
- 이번 구간의 속도가 아닌 이동·회전량을 얻었다는 점을 결과의 m·rad 단위로 구분한다.
  같은 결과를 다음 33페이지의 미드포인트 적분에 넘겨 (x, y, θ)를 갱신한다.

## 33페이지 — 중간 방향으로 이동량을 나누고 현재 상태에 더하기

- 먼저 θ중간 = θ현재 + Δθ/2를 구한다. 회전하면서 이동하면 몸체의 방향도 바뀌므로,
  시작 방향과 끝 방향 사이의 중간 방향으로 이번 이동을 근사한다는 이유를 함께 설명한다.
- 다음 위치는 x다음 ≈ x현재 + Δs cos θ중간, y다음 ≈ y현재 + Δs sin θ중간이며,
  다음 방향은 θ다음 = θ현재 + Δθ다. 현재·다음 표기로 갱신 전후를 구분해
  θ를 먼저 덮어쓴 값이 x·y 계산에 섞이지 않게 한다. 위 식은 각도를 연속적인 값으로
  취급한 형태이며, 방향 범위를 정규화하는 작업은 새 상태를 구한 뒤 적용할 수 있다.
- 32페이지의 Δs = 0.05 m, Δθ = 0.04 rad와 시작 (x, y, θ) = (0, 0, 0)을 사용한다.
  θ중간 = 0 + 0.04/2 = 0.02 rad이고, x다음 ≈ 0.05 cos 0.02 ≈ 0.04999 m,
  y다음 ≈ 0.05 sin 0.02 ≈ 0.00100 m, θ다음 = 0.04 rad다. 삼각함수의 입력은 rad다.
- 그림은 원호의 시작·중간·끝에서 접선 방향을 보여준다. 차이를 읽기 쉽도록 회전각을
  크게 그린 개념도라는 점을 표시하며, 숫자 예제의 0.04 rad를 축척대로 그린 그림은 아니다.
  청록색 중간 화살표는 방향을 뜻하고 실제 이동 거리를 나타내는 선분이 아니다.
- 작은 Δθ의 측정 구간에 적용하는 미드포인트 근사임을 명시한다. 구간 내 속도가 일정한
  원호에서도 Δs는 호의 길이이므로 이를 중간 방향으로 투영하는 위치 식은 근사다.
  완전 정지와 직진에서는 방향 변화가 없고 같은 식이 그대로 적용된다.
- 이 계산을 측정 구간마다 반복해 이전 추정 상태에 변화량을 더하는 것이 누적(적분)이다.
  다음 시연 내부에서 정확한 원호 적분을 쓴다는 구현 설명은 이 페이지 본문에서 덜어내고,
  아래 모델 기준과 시연 문서에 유지한다.

## 모델과 설명의 기준

- 평면에서 바퀴가 미끄러지지 않는 이상적인 차동 구동 모델을 사용한다. `L`은 좌우 바퀴 중심 간격, `r`은 바퀴 반지름이다.
- `ωL`, `ωR`는 바퀴 각속도(rad/s), `vL = rωL`, `vR = rωR`는 바퀴 선속도(m/s)다. 로봇 중심 속도는 `v = (vR + vL)/2`, `ω = (vR − vL)/L`이다.
- 역기구학은 `ωL = (v − Lω/2)/r`, `ωR = (v + Lω/2)/r`이다. 좌우 바퀴의 양의 속도는 로봇 전방을 뜻하며, 모터·엔코더 부호와 감속비는 그 기준에 맞춰 환산한다.
- 본문의 적분은 원자료의 미드포인트 근사다. x·y는 모두 갱신 전 θ로 구한 중간 방향을 사용해 계산한다. 주행 시연은 일정한 v·ω의 원호를 정확히 적분하며, 직진 극한에서도 안정적인 sinc 형태를 사용한다.
- 명령 속도를 적분한 예측과 센서로 실제 이동을 확인한 오도메트리를 구분한다. 궤적 시연은 잡음·미끄럼·모터 지연을 포함하지 않는다. 드리프트 그림은 개념도다.
- frame은 좌표를 읽는 기준이다. TF 트리의 `부모 → 자식` 표기는 부모 프레임에서 본 자식의 위치·방향 관계이며, 점을 다른 기준으로 표현하려면 관계를 합성하거나 역변환한다.
- TF 예시는 센서와 몸체의 축이 평행하다는 조건이다. 센서의 `(1, 0)`은 몸체에서 `(1.3, 0)`, 몸체가 odom에서 `(2, 1, 90°)`이면 odom에서 `(2, 2.3)`이다.
- 평면 주행은 roll = pitch = 0, yaw = θ로 두고 `q = (0, 0, sin(θ/2), cos(θ/2))`를 사용한다. 성분 x·y·z는 위치 좌표가 아니다. ROS 순서는 x·y·z·w이며, 다른 라이브러리는 순서가 다를 수 있다.

## 공식 자료와 정합성

- [REP-103: 단위와 좌표축](https://github.com/ros-infrastructure/rep/blob/master/rep-0103.rst): SI 단위, 몸체 x 전방·y 왼쪽·z 위쪽, 오른손 좌표계와 양의 yaw 방향을 따른다.
- [REP-105: 이동 로봇의 프레임](https://github.com/ros-infrastructure/rep/blob/master/rep-0105.rst): odom의 연속성과 누적 오차, map 기준 추정의 보정 가능성, base_link의 몸체 고정 관계를 구분한다. base_link 원점은 플랫폼의 설계 기준이며 반드시 기하학적 중심일 필요는 없다.
- [ROS 2 Jazzy tf2 개념](https://github.com/ros2/ros2_documentation/blob/jazzy/source/Concepts/Intermediate/About-Tf2.rst): TF는 시간에 따른 프레임 관계를 관리한다. 위치를 측정하는 센서가 아니며, 측정 시각에 맞는 변환이 필요하다.
- [ROS 2 Jazzy 정적 TF](https://github.com/ros2/ros2_documentation/blob/jazzy/source/Tutorials/Intermediate/Tf2/Writing-A-Tf2-Static-Broadcaster-Py.rst): 고정 장착 센서는 정적 변환으로 설명한다. 움직이는 관절에 붙은 센서는 별도 동적 관계가 필요하다.
- [ROS 2 Jazzy quaternion 기초](https://github.com/ros2/ros2_documentation/blob/jazzy/source/Tutorials/Intermediate/Tf2/Quaternion-Fundamentals.rst): ROS 성분 순서, 단위 길이, 항등 회전 `(0, 0, 0, 1)`, RPY 변환을 따른다.
- [nav_msgs/msg/Odometry 정의](https://github.com/ros2/common_interfaces/blob/jazzy/nav_msgs/msg/Odometry.msg): pose는 `header.frame_id`, twist는 `child_frame_id` 기준이다. 본문의 odom·base_link는 해당 필드에 넣는 예시 이름이며 메시지 형식 자체가 강제하는 이름은 아니다.

Odometry와 `odom → base_link` TF를 함께 발행할 때 같은 추정 상태·시각·프레임 이름을 사용한다. 같은 TF 연결은 하나의 발행 주체가 담당하며, Odometry 메시지만으로 TF가 자동 생성되지는 않는다.

## 조작과 검증

34페이지의 두 슬라이더는 바퀴 각속도를 조절한다. 값을 바꾸면 시작 위치로 돌아가 새 예상 경로를 표시하고 일시정지한다. 재생 버튼으로 6초의 이동을 볼 수 있다. 직진·왼쪽 선회·제자리 회전 프리셋도 제공한다. 현재 슬라이드에서만 애니메이션을 실행하며, 동작 줄이기 설정에서는 자동 재생을 시작하지 않는다.

41페이지에서는 yaw를 −180°~180°로 바꾸며 방향 화살표와 quaternion 네 성분을 확인한다. 두 시연 모두 키보드로 조작할 수 있다.

계산 검증: `node tools/check-mobile-model.cjs`. 225개 정·역기구학 왕복 조합, 직진·제자리 회전·원운동, 작은 각속도에서의 적분, 미드포인트 예제, TF 점 변환, quaternion 단위 길이와 yaw 왕복을 확인한다.
