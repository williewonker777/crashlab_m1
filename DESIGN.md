# 크래쉬랩 M1 강의 덱 — 디자인 기획서 v3
2026-08-21. **v1·v2·v2.1 전부 폐기. 이 문서가 유일한 정본이다.**

## 0. v2가 실패한 이유

내가 슬라이드 형식을 **하나로 못 박았다.** 그 결과:

- 75장이 전부 `eyebrow → 제목 → 가운데 그림 → 하단 회색 박스 3개` 로 똑같다
- 화면 위아래 25%씩이 **비어 있고** 내용은 가운데 절반에만 몰려 있다
- 모든 요소가 **선(outline)뿐**이다. 채운 면이 없어 화면이 창백하다
- 실습 슬라이드는 "작성 예정"이 5번 반복돼 **미완성 페이지처럼** 보인다
- 다이어그램이 디자인된 그래픽이 아니라 **와이어프레임 스케치** 수준이다

형식을 하나로 고정한 것이 근본 원인이다. 아래로 대체한다.

## 1. 그리드와 리듬 — 먼저 여백부터 고친다

1920×1080 기준.

- 좌우 마진 **120px**, 12칼럼 그리드, 거터 32px
- **콘텐츠는 세로 80%를 채운다.** 지금처럼 가운데 절반에 몰지 않는다
- 제목 블록 baseline은 상단에서 **200px** 고정 (전 슬라이드 공통)
- 본문 영역은 **y=320 ~ y=980**. 이 안을 채운다
- 하단 슬라이드 번호 외에는 y>1000 에 아무것도 두지 않는다

## 2. 레이아웃 6종 — 하나가 아니라 여섯

내용에 맞는 것을 고른다. 같은 레이아웃이 3장 연속 나오면 안 된다.

### L1 · 타이틀
한양 블루 **단색 전면**. 흰 글자. 강의 번호를 96px로 크게, 제목 72px, 목표 한 줄 34px.
유일하게 배경이 남색인 슬라이드다.

### L2 · 질문
연한 그린 틴트 배경. 질문만 **56px로 화면 중앙**에. 다른 요소 일절 없음.
말 그대로 비워서 강사가 학생을 쳐다볼 시간을 만든다.

### L3 · 다이어그램 지배
**그림이 화면의 70%를 차지한다.** 폭 1400px 이상, 높이 600px 이상.
설명은 하단 박스가 아니라 **그림 위에 직접 주석**으로 붙인다(리더선 + 점).
텍스트는 그림 아래 한 줄만.

### L4 · 분할
좌 5칼럼 개념 텍스트 / 우 7칼럼 그림. 좌우 정렬 baseline을 맞춘다.
텍스트가 그림을 설명해야 하는 경우에 쓴다.

### L5 · 비교
좌우 두 덩어리. **각 덩어리에 서로 다른 배경**(왼쪽 `#F5F7FA`, 오른쪽 `#EDF6E3`)을 깔아
경계를 색으로 만든다. 가운데 vs 구분자.

### L6 · 실습
아래 §5 참조. 워크시트로 다시 만든다.

## 3. 다이어그램 시각 언어 — 와이어프레임 탈피

지금 그림들은 선만 있어서 창백하다. 다음을 규칙으로 한다.

### 3-1. 면을 채운다
- 주요 노드는 **한양 블루 단색 채움 + 흰 글자**. 테두리만 있는 박스로 두지 않는다
- 보조 노드는 `#EDF6E3` 채움 + 남색 글자
- 배경 영역(맵, 그래프 판)은 `#F5F7FA` 채움 + 테두리 없음

### 3-2. 선 굵기는 두 가지만
- **2px** 구조선(축, 격자, 보조)
- **5px** 강조선(주 흐름, 경로, 데이터)
- 그 사이 굵기를 쓰지 않는다. 일관성이 정돈감을 만든다

### 3-3. 주석은 리더선으로
라벨을 도형에 붙이지 말고 **작은 채운 원(r=5) + 2px 리더선 + 라벨** 로 뺀다.
선이 글자를 관통하는 문제가 구조적으로 사라진다.

### 3-4. 숫자는 다르게 다룬다
수치(비용 18, 2.6 m, ±5%)는 **28px bold + 남색**, 단위는 20px regular 회색.
숫자가 그림의 결론인 경우가 많으므로 눈에 먼저 들어와야 한다.

### 3-5. 그림 판
모든 다이어그램은 `#FAFAFA` 카드 위에 올린다. radius 16px, 안쪽 여백 48px.
그림이 허공에 뜨지 않게 한다.

## 4. 하단 회색 박스 3개를 없앤다

전 슬라이드에 반복돼 가장 단조로운 요소다. 대신 셋 중 하나를 쓴다.

1. **핵심 한 줄** — 40px, 남색, 좌측 정렬. 그림 아래 한 문장만
2. **그림 위 주석** — 설명을 다이어그램 안으로 옮긴다 (L3에서 기본)
3. **2단 대비** — 비교가 필요할 때만 좌우 두 덩어리

3개를 나란히 놓는 배치는 **비교 슬라이드에서만** 허용한다.

## 5. 실습 슬라이드 — 워크시트로

지금은 "작성 예정" 5개가 깔려 미완성처럼 보인다. 이렇게 바꾼다.

- 상단에 **그린 단색 헤더 바**(높이 80px) + 흰 글자로 실습 제목
- 본문은 **흰 배경 워크시트**. 배경 틴트를 깔지 않는다
- "작성 예정" 문구를 **전부 제거**한다. 대신 빈 줄(밑줄 2px `#E5E5E5`)만 둔다
- 단계는 큰 숫자 뱃지(원, 그린 채움, 흰 숫자 32px) + 빈 줄
- 빈칸 주석도 실습 슬라이드와 함께 제거한다.

빈 칸이 "아직 안 만든 것"이 아니라 "채우라고 만든 자리"로 보여야 한다.

## 6. 강의별 악센트

4개 덱이 구분되도록 강의마다 보조색을 하나씩 준다. 주색(한양 블루)은 공통.

| 강의 | 악센트 | 쓰는 곳 |
|---|---|---|
| ① 로보틱스·ROS2 | `#75B446` 그린 | eyebrow, 진행바, 강조 |
| ② 조작·제어 | `#00A0B0` 청록 | 〃 |
| ③ VSLAM | `#F7931E` 오렌지 | 〃 |
| ④ VLA | `#7B5EA7` 보라 | 〃 |

악센트는 **강조에만** 쓴다. 면적을 많이 차지하면 안 된다.

## 7. 타이포 위계 (프로젝터 기준 유지)

| 요소 | 크기 | 굵기 |
|---|---|---|
| 강의 번호 (L1) | 96px | 700 |
| 슬라이드 제목 | 64px | 700 |
| 질문 (L2) | 56px | 700 |
| 핵심 한 줄 | 40px | 600 |
| 본문·라벨 | 28px | 400 |
| 수치 | 28px | 700 |
| 다이어그램 라벨 | 22px | 500 |
| eyebrow | 18px | 600 · letter-spacing .12em |

## 8. 유지할 것

- 슬라이드 59장 구성(16/14/15/14)과 강의 슬롯 순서
- 한양 블루 `#1D2475` 주색
- 인라인 SVG, 외부 요청은 Google Fonts만
- 키보드 네비게이션, 접근성 속성
- 겹침·가림 0건 (tools/audit-overlap.html, tools/audit-occlusion.html)

---

# v4 개정 — 구성(composition) 원칙
2026-08-21. 사용자 지정 레퍼런스(Wix "2025 최고의 웹사이트 디자인 31") 분석 반영.
**이 절이 §2 레이아웃보다 우선한다.**

## A. 지금까지 실패한 진짜 이유

부품(색·타이포·컴포넌트)은 v2·v3·SVG규격으로 정리됐다. 그런데도 어색한 이유는 **구성**이다.

강의2 s8 시안에서 확인:
- 순환 개념을 **가로 파이프라인 + 리턴선**으로 그려 순환으로 안 읽힌다
- 가운데 원판이 아무것과도 연결되지 않는다 (Tufte 가 말한 chartjunk)
- 세로 400px이 선 하나로 채워진 빈 공간이다
- 노드 하나가 viewBox 밖으로 잘렸다

## B. 레퍼런스에서 가져올 것

### B-1. 비대칭으로 균형 잡기 ★ 가장 중요

> "최고의 홈페이지 디자인 대부분이 대칭 레이아웃을 쓰지만 OK Drugs는 **비대칭 방식으로 균형**을 이루었다.
>  콜라주 레이아웃로 자유로운 분위기를 표현하면서도 **충분한 여백**을 주어 압도되지 않게 했다."

지금 우리 슬라이드는 **전부 좌우 대칭 · 중앙 정렬**이다. 그래서 75장이 똑같아 보인다.

적용:
- 그림을 화면 중앙에 두지 말고 **한쪽으로 밀고 반대쪽에 여백이나 텍스트**를 둔다
- 제목·그림·핵심문장을 **같은 축에 정렬하지 않는다.** 제목은 왼쪽 위, 그림은 오른쪽 아래처럼 어긋나게
- 대신 **시각적 무게는 맞춘다.** 큰 그림 반대편에는 큰 여백 또는 굵은 짧은 문장

### B-2. 요소가 반복되지 않게

> "이 크리에이터는 다양한 옵션·레이아웃·타이포그래피를 탐구했고 **요소들이 반복되지 않도록** 했다."

적용: 연속한 3장이 같은 구성을 쓰면 안 된다. 그림 위치(좌/우/전면), 제목 위치, 텍스트 배치를 번갈아 쓴다.

### B-3. 여백을 두려워하지 않기

> "콘텐츠 주위에 공간 남기는 것을 두려워하지 마세요."

단 **의도된 여백**과 **버려진 공간**은 다르다.
- 좋은 여백: 요소 한쪽에 몰아서 시선을 유도하는 빈 영역
- 나쁜 여백: 그림 한가운데가 비어 요소들이 흩어져 보이는 것 ← 지금 s8이 이 경우

**그림 안에 200px 이상 연속으로 빈 영역이 있으면 배치 실패다.**

### B-4. 얇은 선을 모티브로

> "로고가 사이트 디자인에 영향을 미치고 **얇은 선을 모티브**로 사용하는 점이 좋았다."

적용: 강의별 악센트색 얇은 선(2px)을 슬라이드 전체를 관통하는 모티브로 쓴다.
제목 아래 짧은 선, 그림 카드 상단 선, 핵심문장 앞 선 — 같은 선 언어를 반복해 통일감을 만든다.

### B-5. 잡지 표지형 헤더

> "자신의 디자인을 커버 이미지로 활용해 **잡지 표지처럼 보이는 헤더**를 만들고,
>  제목을 이미지 곡선에 맞춰 정렬했다. 오른쪽 앵커 포인트가 목차 역할을 한다."

적용: 각 강의 타이틀 슬라이드(L1)를 잡지 표지처럼 만든다.
큰 숫자, 큰 제목, 그리고 그 강의에서 나올 **다이어그램 하나를 크게 깔아** 예고편으로 쓴다.

### B-6. 모서리를 쓴다

> "4개 모서리에 연락처·SNS 링크 버튼을 배치했다."

적용: 슬라이드 모서리(현재 우하단 번호만 있음)를 활용한다.
좌하단에 강의 번호와 섹션명을 얇게 넣으면 강사가 위치를 파악하기 쉽다.

## C. 그림 폭 3등급 (Distill 방식)

모든 그림을 같은 폭 카드에 넣지 않는다. **중요도가 폭을 정한다.**

| 등급 | 폭 | 쓰는 곳 |
|---|---|---|
| `full` | 1600px (카드 전체) | 그 강의의 핵심 개념 1~2개 |
| `wide` | 1100px | 일반 개념 |
| `half` | 760px | 보조 설명. 반대편에 텍스트나 여백 |

`half` 를 쓸 때가 **비대칭 구성의 기회**다. 적극적으로 써라.

## D. 구성 패턴 — 내용의 형태가 배치를 정한다

무엇이든 가로로 늘어놓지 마라.

| 내용 | 배치 |
|---|---|
| 순환 · 반복 | **원형**. 4~5개 노드를 원주에 놓고 곡선 화살표로 잇는다 |
| 순서 · 흐름 | 가로 일렬 |
| 계층 · 분해 | 세로 트리 |
| 비교 · 대립 | 좌우 2분할, 각 영역에 다른 배경 |
| 시간 · 누적 | 가로축 그래프 |
| 공간 · 지도 | 격자 |

강의2 s8(SLAM 한 바퀴)은 **순환**이므로 원형 배치가 맞다. 지금의 가로 파이프라인은 틀렸다.

## E. 시선 순서를 설계한다

각 그림에서 **가장 먼저 볼 요소 하나**를 정하고, 그것에만 강조를 준다.
- 강조 수단은 하나만: 크기 **또는** 색 **또는** 위치. 셋을 동시에 쓰지 않는다
- 나머지 요소는 톤을 낮춘다
- 지금은 모든 노드가 같은 무게라 어디를 먼저 볼지 지정돼 있지 않다

## F. chartjunk 금지

정보를 담지 않는 장식을 넣지 않는다.
- 연결되지 않은 라벨·원판 (s8의 "SLAM" 원판)
- 의미 없는 색 구분 (4개 중 하나만 다른 색인데 이유가 없는 것)
- 장식용 그림자·테두리

---

# v5 — 레퍼런스 사이트에서 차용할 9가지 장치
2026-08-21. 사용자 지정 목록(Wix 선정 31선)의 실제 사이트를 렌더해서 분석했다.
**추상적 원칙이 아니라 구체적 장치다. 그대로 가져다 쓴다.**

## 1. 칩 라벨 — 주석 문제의 최종 해법 ★

**출처: Bhroovi Gupta.** 이미지 콜라주 위에 `self-starter` `data-driven` 같은 라벨을
**흰 배경 + 1px 테두리 + 둥근 모서리 칩** 안에 넣어 얹었다.

우리가 계속 싸운 문제(선이 글자를 가림, 헤일로가 지저분함)를 이게 끝낸다.
어떤 선·도형 위에 올려도 읽힌다.

```svg
<g class="chip">
  <rect x="0" y="0" width="176" height="44" rx="22"
        fill="#FFFFFF" stroke="#D8DEE9" stroke-width="1"/>
  <text x="88" y="22" text-anchor="middle" dominant-baseline="central"
        font-size="22" fill="#1D2475">여기서 오차 발생</text>
</g>
```

**다이어그램 안의 모든 설명 라벨은 칩으로 한다.** paint-order 헤일로는 폐기.
축 라벨(x, y, z)처럼 아주 짧은 것만 예외로 맨텍스트 허용.

## 2. 거대 헤드라인 + 우하단 소항목 3열

**출처: Bhroovi Gupta.** "I'm many things." 를 화면 절반 크기로 놓고
오른쪽 아래에 해시태그를 3열로 작게 깔았다. 큰 것 하나 : 작은 것 여럿의 대비.

적용: **질문 슬라이드(L2)**.
질문을 화면 절반 크기로 왼쪽에 놓고, 우하단에 그 강의에서 다룰 키워드를 3열로 작게.

## 3. 크기 대비만으로 위계 만들기

**출처: Bhroovi Gupta.** 흰 배경 + 검정 타이포만 쓰고도 강하다. 색을 거의 안 쓴다.

적용: 색으로 강조하려 들지 말고 **크기 차이를 키워라.**
제목 64px / 본문 28px 은 차이가 부족하다. 강조할 것은 과감하게 2배 이상 키운다.

## 4. 우측 번호 인덱스 + 가는 실선

**출처: Sonja Van Dulmen.** 화면 오른쪽 가장자리에 `01 02 03 04 05`,
각 번호 위에 **가는 실선**, 아래에 두 줄 라벨. 목차가 곧 디자인 요소다.

적용: **타이틀 슬라이드(L1)** 오른쪽에 그 강의의 개념 목록을 이 형태로.
예고편이자 목차 역할을 한다. 지금 타이틀 슬라이드가 허전한 문제도 같이 해결된다.

## 5. 자간 대비

**출처: Sonja Van Dulmen.** 큰 제목은 자간을 좁히고, 작은 캡션은 극단적으로 넓힌다.
크기만이 아니라 **자간으로도 위계**를 만든다.

| 요소 | letter-spacing |
|---|---|
| 큰 제목 | `-0.02em` |
| eyebrow · 영문 라벨 | `0.18em` |
| 칩 라벨 | `0` |

## 6. 네 모서리에 기능 배치

**출처: Daniel Aristizabal.** 좌상단 로고, 우상단 메뉴, 좌하단·우하단 링크.
**가운데는 콘텐츠, 모서리는 기능.**

적용: 좌하단에 `강의 ② · CONTROL` 처럼 현재 위치를 얇게. 우하단은 현재대로 슬라이드 번호.

## 7. 과감한 단색 여백

**출처: Daniel Aristizabal.** 화면의 60%가 단색이고 요소는 흩어져 있다.
빈 공간을 없애려 하지 않고 **무기로 쓴다.**

적용: 그림을 한쪽으로 밀고 **반대쪽을 통째로 비운다.**
단 §v4 B-3 의 구분을 지켜라 — 그림 *안*이 비는 건 실패, 그림 *바깥*이 비는 건 의도.

## 8. 동일 폭 알약 3개

**출처: In Print.** `ABOUT / ARCHIVE / CONTACT` 를 같은 폭 알약으로 나란히.

적용 대상이던 실습 슬라이드는 v8에서 삭제됐다.

## 9. 제목 아래 짧은 굵은 선

**출처: In Print.** 제목 폭보다 짧은 굵은 선을 제목 바로 아래.

적용: 슬라이드 제목 아래 **폭 80px · 굵기 4px** 악센트색 선.
§v4 B-4 의 "얇은 선 모티브"와 함께 쓴다 — 굵은 선은 제목용, 얇은 선(2px)은 인덱스·구분용.

## 적용 우선순위

1. **칩 라벨** — 전 다이어그램. 가장 큰 개선
2. **타이틀 슬라이드 우측 인덱스** — 4장
3. **질문 슬라이드 대비 구성** — 9장
4. 실습 슬라이드는 v8에서 삭제
5. 나머지(자간·모서리·제목선)는 CSS 일괄 적용

---

# v6 — 실물 ALICE M1 사진 통합
2026-08-21. 사용자가 누끼 PNG 7장을 제공. `assets/img/` 에 최적화해 배치했다.

## A. 자산

| 파일 | 크기 | 원본 |
|---|---|---|
| `m1-side.png` | 648×1400 | 정측면 90° 전신 |
| `m1-3q.png` | 1112×1400 | 3/4 전신 |
| `m1-sensor.png` | 1200×1055 | 상체·머리 클로즈업 |
| `m1-arm-ext.png` | 1086×1200 | 팔 뻗은 자세 |
| `m1-arm-fold.png` | 1077×1200 | 팔 접은 자세 |
| `m1-gripper.png` | 1200×1127 | 그리퍼 + 큐브 |
| `m1-top.png` | 1000×1000 | 위에서 본 컷 |

전부 RGBA 알파. 합계 3.1MB.

## B. 반드시 지킬 것 — 흰 로봇 문제 ★

**M1 은 본체가 흰색이다. 흰 배경에 그냥 올리면 사라진다.**

로봇 이미지는 다음 셋 중 하나 위에만 놓는다.

1. **한양 블루 단색 패널** — 대비 최고. 타이틀·강조 슬라이드에
2. **`#F5F7FA` 틴트 패널** — 일반 개념 슬라이드에. 반드시 패널을 깔고 그 위에
3. **드롭섀도** — `filter: drop-shadow(0 8px 24px rgba(29,36,117,.18))`
   패널 없이 흰 배경에 놓아야 할 때만. 단독으로는 약하니 1·2와 병행 권장

**맨 흰 배경 위 직접 배치 금지.**

반대로 **관절은 검은색**이라 흰 본체와 대비가 크다. 관절 위치에 주석을 달면 잘 보인다.
이걸 적극 활용해라 — 링크·관절·자유도 설명이 사진 하나로 해결된다.

## C. 배치 계획

| 슬라이드 | 이미지 | 방식 |
|---|---|---|
| 각 강의 **타이틀(L1)** | `m1-3q.png` | 남색 전면 우측에 크게. 우측 인덱스와 겹치지 않게 배치 조정 |
| ①-3 링크와 관절 | `m1-side.png` | 좌측 배치, **검은 관절 위에 칩 라벨 + 리더선**. 기존 막대기 SVG 폐기 |
| ①-4 자유도 | `m1-side.png` | 같은 이미지, 관절 3곳에 회전 호 겹치기 |
| ①-5~8 좌표계·TF | `m1-3q.png` | 골반에 베이스 프레임, 그리퍼에 툴 프레임 축을 SVG 로 겹침 |
| ①-9 센서 개요 | `m1-sensor.png` | 머리 카메라 위치에 칩 라벨. 기존 4분할 아이콘 폐기 |
| ②-7 관절 한계 | `m1-arm-ext.png` | 어깨 관절에 가동범위 호 |
| ②-11~13 기구학·작업공간 | `m1-arm-ext` + `m1-arm-fold` | **좌우 나란히 비교**. 같은 크기·같은 baseline |
| ②-14 특이점 | `m1-arm-ext.png` | 완전히 뻗은 상태 강조 |
| ③-11 코스트맵 | `m1-top.png` | 격자 위 로봇 표시. 기존 초록 원 대체 |
| ④-3 VLA 개념 | `m1-gripper.png` | Action 블록에 실제 파지 장면 |
| ④-7~10 에피소드 | `m1-gripper.png` | 타임라인 트랙 옆 참조 이미지 |

## D. 사진과 SVG 를 겹치는 방법

사진 위에 주석을 다는 것이 이 작업의 핵심이다.

```html
<div class="photo-figure">
  <img src="assets/img/m1-side.png" alt="ALICE M1 측면">
  <svg class="photo-overlay" viewBox="0 0 648 1400" preserveAspectRatio="xMidYMid meet">
    <!-- 좌표계가 이미지 픽셀과 1:1 이어야 주석이 안 어긋난다 -->
    <circle cx="..." cy="..." r="6" fill="#75B446"/>
    <line .../>
    <g class="chip">...</g>
  </svg>
</div>
```

- `viewBox` 를 **이미지 원본 픽셀 크기와 동일하게** 잡는다. 그래야 좌표를 픽셀로 읽어 찍을 수 있다
- `img` 와 `svg` 를 같은 컨테이너에 절대배치로 겹치고 둘 다 `width:100%`
- 관절 좌표는 이미지를 실제로 열어 확인하고 찍어라. 추측하지 마라

## E. 기존 SVG 를 지울 곳 / 남길 곳

**대체(사진으로 교체)**: 링크와 관절, 자유도, 센서 개요, 코스트맵의 로봇 표시
**유지(개념도라 사진이 못 대신함)**: SLAM 순환, ROS2 노드-토픽, PID 응답, 학습·추론 파이프라인, 에피소드 타임라인

사진이 항상 나은 게 아니다. **물리적 실체가 있는 것만** 사진으로 바꾼다.
추상 개념(순환·흐름·파이프라인)은 SVG 가 맞다.

---

# v8 — 강의 운영 구조 개정
2026-08-22. 사용자 직접 지시. 이 절이 기존 실습·정리·강의 순서 규칙보다 우선한다.

- 실습 12장과 정리 4장을 삭제한다.
- 최종 슬라이드 수는 **16 / 14 / 15 / 14**, 총 **59장**이다.
- 강의 슬롯은 ① 로보틱스·ROS2(9/11) → ② 조작·제어(9/18) → ③ VSLAM(10/2) → ④ VLA(10/16) 순서다.
- 악센트는 콘텐츠가 아니라 슬롯을 따른다: ② 청록, ③ 오렌지.
- `/cmd_vel`은 내비게이션의 측위→계획→제어 루프가 실제 주행 명령으로 끝나는 연결 개념으로 강의 ③ 마지막에 둔다.
- 실습 100분은 덱 밖에서 진행하며 instructor 페이지에서만 시간 배분을 관리한다.


# v9 — 강의 ③ VSLAM 심화 확장
2026-08-31. 강의 ③의 2시간 이론 분량을 확보하기 위해 v8의 `16 / 14 / 15 / 14 = 59장` 구성을 `16 / 14 / 47 / 14`로 확장한다.

- 강의 ③은 VSLAM 센서·원리·지도·Nav2 연동·실패 사례를 47장으로 다룬다.
- 사용자 제공 VSLAM 캡처와 매핑 영상은 해당 개념 슬라이드에 직접 배치한다.
- v8의 강의 순서·악센트·운영 규칙을 포함한 다른 규칙은 그대로 유지한다.

# v10 — 강의 ③ 가독성·정확성 교정
2026-08-31. 강의 검토 피드백에 따라 확인 문제와 요약 슬라이드를 삭제하고,
기술 설명과 미디어 배치를 다시 정리했다.

- 원본 캡처는 작은 썸네일로 나열하지 않는다. 내부 글자를 읽어야 하는 자료는
  슬라이드 폭에 가깝게 확대하거나 핵심 영역만 크게 크롭한다.
- 비교 슬라이드는 양쪽 카드의 제목·본문 시작선과 정보량을 맞춘다.
- `map → odom`은 전역 보정을, `odom → base_link`는 연속적인 로컬 움직임을 맡는다.
- IMU·Stereo·Costmap 입력·Controller 메시지는 센서 조건과 ROS 2 설정을 함께 밝힌다.
- 강의 ③은 47장, 전체 강의는 98장이다.

# v11 — 강의 ② 서론 개정
2026-09-04. 서론이 곧바로 모터 제어로 들어가지 않도록 질문을 "제어란 무엇인가?"로 바꾸고,
L3 제어 루프(폐루프 블록도)·L3 A·B·C 예시·L5 제어 용어(concept-table)·L5 개루프 vs 폐루프 4장을 삽입했다. 이후 본문은 내용 그대로 두 장 뒤로 민다.

- 제어의 정의는 "목표와 현재의 차이를 줄이도록 입력을 정하는 일"로 한 문장에 못 박고, 이 루프의 제어기 상자를 PID 장에서 연다.
- 개루프도 제어다. 폐루프의 장점은 외란과 모델 오차를 다시 줄이는 것이며, 목표로의 완전 복귀는 I 항 등 조건이 붙으므로 단정하지 않는다.
- `deck.css`의 슬라이드 번호 선택자(`#slide-N`)는 반드시 덱 클래스(`.lecture-3` 등)에 스코프한다. 스코프 없는 v10 규칙이 재번호 뒤 강의 ② 18장의 패딩을 지운 회귀가 있었다.
- 강의 ②는 20장, 전체 강의는 102장이다.

# v12 — 강의 ② PID 설명과 게인 조절 예시
2026-09-07. 사용자 제공 `2024 PID Control.pptx`를 참고해 기존 PID 3장을 9장으로 확장했다.

- 8~16페이지: 개요 → P 원리·부하 오차 → I 원리·와인드업 → D 원리·잡음 → 응답 지표 → 게인 조절.
- 각 항은 수식, 관절 숫자 예시, 게인의 효과, 한계를 함께 설명한다.
- 게인 조절은 같은 1축 모델을 재계산하며 위치 응답·입력 토크·측정 지표를 갱신한다.
- 그래프 조건과 출처, 원본에서 교정한 설명은 `PID.md`에 기록한다.
- 새 스타일은 `.lecture-2 .pid-*` 클래스로 한정한다. 슬라이더 방향키는 슬라이드 이동에 사용하지 않는다.
- 강의 ②는 26장, 전체 강의는 108장, OT 포함 총 120장이다.

# v13 — 강의 ② 목차 정합성
2026-09-07. 질문 페이지의 목차는 표지의 네 항목과 번호·제목·설명을 동일하게 사용한다.

- 01 모터 제어 — 위치 · 속도 · 토크
- 02 오차 제어 — PID
- 03 이동 제어 — 차동 구동
- 04 팔 제어 — 정기구학 · 역기구학
- 질문 페이지 2·20·22에서는 각각 01·03·04를 강조한다. 현재 항목에는 `aria-current="step"`을 붙인다.
- 데스크톱에서는 하단 4열, 작은 화면에서는 2열로 배치한다.

# v14 — 강의 ② P·PI·PID 비교 예시
2026-09-07. 15페이지에서 P와 PI의 차이가 작게 보이는 문제를 수정했다.

- 공통 Kp=0.10, Ki=0.02, Kd=0.04와 감쇠 b=0.05인 모델을 사용한다.
- P의 남는 오차, PI의 오차 보정과 넘침, PID의 진동 완화를 그래프와 계산값으로 설명한다.
- 세 응답의 모델·초기 조건·부하·출력 한계는 동일하다. P는 점선으로도 구분한다.
- 16페이지의 기본값과 프리셋을 비교 예시에 맞춘다. 10페이지의 P 게인 세 값은 그대로 사용한다.

# v15 — 강의 ② 게인 실험 하단 가독성
2026-09-07. 16페이지에 밀집한 지표와 설명을 정리했다.

- 응답 지표 네 개를 여백이 있는 칸으로 나누고, 이름과 숫자 사이 간격을 넓힌다.
- 정착 상태는 한 줄로 표시하고, 출력 포화 비율은 적분 누적 방지 설정 옆에 둔다.
- 모델·목표 각도·부하·토크 한계를 하단의 개별 항목으로 배치한다.
- 게인 단위는 슬라이더 옆에 표시한다. 작은 화면에서는 지표와 실험 조건을 2열로 배치한다.

# v16 — 강의 ② 피드백 예시와 관절 드라이브 설명
2026-09-08. 6페이지에 같은 부하 변화에 대한 개루프·폐루프 예시를 추가하고,
17페이지의 stiffness·damping 설명을 교정했다.

- 컨베이어 모터의 목표는 60 rpm, 부하 증가 직후 속도는 40 rpm인 설명용 상황이다.
- 개루프는 미리 정한 입력을 유지하고, 폐루프는 엔코더 피드백으로 오차를 확인해 입력을 늘린다.
- 폐루프의 회복 정도에는 제어기와 출력 여유가 영향을 주므로 완전 복귀를 단정하지 않는다.
- stiffness·damping은 PhysX 회전 관절 Force drive의 P·D 게인으로 설명한다.
- 적절한 감쇠와 과도한 감쇠를 구분하고, 중력에 의한 정지 오차에는 보상 유무 조건을 붙인다.
- 새 스타일은 `.lecture-2 .loop-*`, `.lecture-2 .drive-*`로 한정한다. 총 26장은 유지한다.

# v17 — 강의 ② 모터 제어 대상과 피드백 순서
2026-09-08. 위치·속도·토크를 먼저 소개한 뒤 개루프·폐루프를 비교하도록 6·7페이지를 교환했다.

- 5페이지 제어 용어 → 6페이지 위치·속도·토크 → 7페이지 개루프 vs 폐루프 → 8페이지 PID.
- 모터에서 제어할 물리량을 소개하고, 속도 피드백 예시를 거쳐 오차로 입력을 정하는 PID로 연결한다.
- 섹션 순서와 `slide-N`, 접근성 번호를 함께 맞춘다. 모터 동작과 비교 예시 내용은 유지한다.

# v18 — 강의 ② PID 챕터 표지
2026-09-08. 다른 세 챕터와 같은 질문 형식으로 ‘02 오차 제어 · PID’ 표지를 추가했다.

- 8페이지: “오차를 보고 입력을 어떻게 정할까?” 질문과 기존 네 항목 목차를 사용하고, 02를 강조한다.
- 7페이지 개루프 vs 폐루프 → 8페이지 PID 표지 → 9페이지 PID 개요로 연결한다.
- 챕터 표지는 2·8·21·23페이지이며, 01·02·03·04를 순서대로 강조한다.
- 기존 8~26페이지는 9~27페이지로 이동한다. PID 실험은 17페이지, stiffness·damping은 18페이지다.
- 강의 ②는 27장, 전체 강의는 109장, OT 포함 총 121장이다. 홈페이지·문서의 수량과 참조도 맞춘다.

# v19 — 강의 ② 표지 문구와 로봇 간격
2026-09-08. 강의명은 ‘제어’로 통일하고, 표지에서 부제와 로봇 어깨가 닿는 배치를 수정했다.

- 데스크톱 표지는 강의 번호·제목과 부제·로봇·목차에 각각 칼럼을 배정한다.
- 로봇을 문구 위에 절대 배치하지 않고, 그림 칼럼과 글자 칼럼 사이의 여백을 확보한다.
- 변경은 강의 ② 표지에 한정한다. 모바일에서는 기존 세로 배치를 사용한다.

# v20 — 강의 ② 모터 제어 기초 보강
2026-09-08. 챕터 1에 구동 과정·엔코더·제어 주기 세 장을 추가했다.

- 6페이지 위치·속도·토크 → 7페이지 명령에서 구동까지 → 8페이지 엔코더 → 9페이지 개루프·폐루프 → 10페이지 제어 주기 → 11페이지 PID 표지.
- 7페이지는 제어기의 계산, 드라이버의 전류 조절, 모터의 토크 발생, 관절의 움직임과 피드백을 구분한다.
- 8페이지는 엔코더 원리 그림과 10°→12°의 측정값으로 0.1초 동안의 평균 각속도 20°/s를 설명한다.
- 10페이지는 측정·오차 계산·입력 갱신과 다음 주기까지의 유지를 시간 순서로 보여준다. 수치는 설명용이다.
- 새 스타일은 `.lecture-2 .motor-basics`, `.motor-actuation-*`, `.motor-encoder-*`, `.motor-cycle-*`로 한정한다.
- 챕터 표지는 2·11·24·26페이지다. PID 설명은 12~20페이지, stiffness·damping은 21페이지다.
- 강의 ②는 30장, 전체 강의는 112장, OT 포함 총 124장이다. 기존 27장 내용은 유지하고 번호와 참조를 갱신한다.

# v21 — 강의 ② 드라이버의 전류 조절 과정
2026-09-08. 7페이지 흐름도 아래에 전류 목표 계산·PWM 스위칭·전류 피드백 설명을 추가했다.

- 드라이버가 제어 칩의 PWM 신호로 전자 스위치를 구동하고, 전원 전압을 권선에 인가해 전류를 조절하는 과정을 설명한다.
- 왼쪽은 순서가 있는 세 단계 설명, 오른쪽은 모터 축 기준의 DC 모터 계산 예시와 전압·전류 파형이다.
- 토크 상수 0.1 N·m/A, 토크 명령 0.2 N·m에서 목표 전류 2 A를 구한다. 실제 로봇의 사양이 아닌 설명용 값이다.
- PWM 전압의 펄스와 권선 전류의 연속적인 변화를 같은 시간축의 개념 그림으로 구분한다.
- 큰 흐름도는 높이를 정리하고 본문과 그림의 여백을 확보한다. `.lecture-2 .motor-current-*`로 스타일을 한정한다.
- 총 30장과 기존 페이지 번호를 유지한다.

# v22 — 강의 ② 엔코더의 신호·각도·방향 설명
2026-09-08. 8페이지를 엔코더 원리, 카운트 환산, 종류 비교로 보강했다.

- 왼쪽은 광학식 증분형의 원판 그림과 A·B 파형이다. 빛의 통과·차단을 전기 신호로 읽고, 두 신호의 선후 관계로 회전 방향을 구분한다.
- 오른쪽은 1회전 3,600카운트인 설명용 예시다. 기준점 0°에서 100·120카운트를 각각 10°·12°로 환산하고, 기존 평균 각속도 20°/s 계산으로 연결한다.
- 하단에서 증분형의 변화량 누적·기준점 설정과 절대형의 위치 코드 판독을 비교한다.
- 원판 무늬 수와 A·B 파형은 원리 그림이다. 오른쪽 분해능 수치나 ALICE M1의 사양을 나타내지 않는다.
- 변경은 `.lecture-2 .motor-encoder-*`에 한정하며 총 30장과 페이지 순서를 유지한다.

# v23 — 강의 ② 엔코더 방향 전환 버튼
2026-09-08. 8페이지에서 A 선행·B 선행을 버튼으로 선택할 수 있게 했다.

- 채널 A·B의 위치와 색은 유지하고 파형의 위상만 바꿔 어느 신호가 먼저 상승하는지 비교한다.
- 예시의 부호는 A 선행을 + 방향, B 선행을 − 방향으로 정한다. 시계·반시계 방향을 특정하지 않는다.
- 오른쪽 계산도 10°→12°·+20°/s와 12°→10°·−20°/s로 전환해 파형과 수치의 방향을 맞춘다.
- 선택 버튼에 `aria-pressed`를 표시하고, 상태 설명과 SVG 대체 설명을 함께 갱신한다. Enter·Space로도 선택할 수 있다.
- 기능은 `assets/js/encoder-demo.js`와 기존 `.motor-encoder-*` 스타일에 한정한다.

# v24 — 강의 ② 엔코더의 빛 통과·차단 시연
2026-09-08. 8페이지 원판을 회전시키고, A 채널의 광경로를 단면으로 보여준다.

- 원판만 회전하고 A·B 감지 위치와 발광부·수광부는 고정한다. 틈이 A 감지 위치를 지나면 광경로가 이어지고, 불투명한 부분에서는 원판 뒤의 빛을 숨긴다.
- 원판의 실제 각도에서 투과 상태를 계산하고, 수광부 표시·신호 0/1·A·B 파형 위의 현재 위치를 함께 갱신한다.
- A/B 선행 버튼은 회전 방향과 파형·계산 예시를 전환한다. 일시정지·재생 버튼으로 원하는 순간을 관찰할 수 있다.
- 8페이지가 활성화되어 있을 때만 재생하며, 다른 페이지나 숨겨진 탭에서는 멈춘다. 동작 줄이기 설정에서는 정지 상태로 시작한다.
- 광학 그림은 느린 원리 시연이며 실제 제품의 분해능·속도·신호 극성을 나타내지 않는다. 모바일에서는 옆으로 밀어 광경로를 볼 수 있다.

# v25 — 강의 ② 평균 각속도 계산 정렬
2026-09-08. 8페이지 평균 각속도 옆 세로 막대를 위쪽의 얇은 구분선으로 바꿨다.

- 설명과 수식의 왼쪽 여백을 없애 위 측정 카드 및 분해능 설명과 시작선을 맞춘다.
- 데스크톱·낮은 화면·모바일에서 같은 정렬을 유지한다.

# v26 — 강의 ② 제어 주기 개념 강조
2026-09-08. 10페이지 제목을 ‘제어 주기: 얼마나 자주 갱신할까?’로 바꾸고, 반복 간격을 중심으로 정리했다.

- 상단 강조 영역에서 제어 주기 Δt를 ‘측정·계산·입력 갱신을 반복하는 시간 간격’으로 정의한다.
- ‘0.1초마다 반복’과 ‘1초에 10회 · 10 Hz’를 연결하고, 갱신 시점 사이에 Δt = 0.1 s를 표시한다.
- 목표 30°와 측정·오차·입력 갱신 예시는 간결하게 유지한다. 모바일에서는 세로 순서 사이에 같은 간격을 표시한다.
- 변경은 10페이지와 `.motor-cycle-*` 스타일에 한정하며, 총 30장과 페이지 순서를 유지한다.

# v27 — 강의 ② 엔코더 개념 강조
2026-09-08. 8페이지 제목을 ‘엔코더: 회전 위치를 읽는 센서’로 바꾸고 센서의 역할을 먼저 설명한다.

- 제목 아래에 회전 위치를 전기 신호로 바꿔 제어기에 전달한다는 정의를 넣고, ‘전기 신호’와 ‘제어기’를 강조한다.
- 하단 종류 설명은 핵심 내용을 짧게 정리해 정의와 원리 그림 사이의 여백을 확보한다.
- 원판·광경로·파형 시연과 방향 선택, 각도·평균 각속도 계산을 유지한다. 총 30장과 페이지 순서는 같다.

# v28 — 강의 ② PID의 비례·적분·미분 설명
2026-09-08. 12페이지 제목과 각 항의 설명에 비례·적분·미분의 수학적 의미를 드러냈다.

- P는 현재 오차에 일정한 게인을 곱하는 비례 관계, I는 오차–시간 그래프의 누적 면적, D는 순간 기울기로 설명한다.
- P의 두 배 관계, I의 주기별 e × Δt 누적과 음수 오차, D의 증가·감소·일정한 오차에 따른 부호를 구분한다.
- 전체 PID 식과 관절 예시의 단위를 유지하고, 세 항에 각각 게인을 곱해 더한다는 관계로 마무리한다.

# v29 — 강의 ② PID 상세 페이지 가독성 정리
2026-09-08. 13~18페이지의 긴 문장을 계산표·과정·조건별 설명으로 바꿨다.

- 13페이지는 현재 각도·오차·P 입력을 표로 비교한다. 14·15·17페이지는 계산 조건, 계산식, 결과를 별도 영역에 정렬한다.
- 16페이지는 포화 → 누적 → 결과의 순서를 드러내고, 조건부 적분에서 누적을 멈추는 경우와 허용하는 경우를 구분한다.
- 18페이지는 잡음의 크기를 수치로 보여주고, Kd·측정값 미분·저역통과 필터의 효과와 한계를 짧은 항목으로 정리한다.
- 각 페이지의 제목과 핵심 한 문장을 짧게 정돈한다. 고정 목표, 정지 평형, 안정성·출력 여유 등 필요한 조건과 단위는 유지한다.
- 기존 그래프와 PID 계산 모델을 유지한다. 새 배치는 `.pid-detail-slide`와 PID 계산·비교 요소에 한정한다.

# v30 — 강의 ② 차동 구동 중심의 챕터 3 확장
2026-09-08. `2025 모바일로봇 제어.pptx`의 Differential Drive Kinematics를 중심으로 챕터 3을 본문 20장으로 확장했다.

- 25~34페이지는 모델·바퀴 단위·정기구학·ICC·역기구학·공간 속도·위치 누적·주행 시연으로 연결한다. 계산용 치수와 수치 예시를 통일한다.
- 35~44페이지는 odometry·frame·TF·quaternion·Odometry 메시지를 앞의 기구학 결과와 연결한다. 명령 예측과 센서 측정, map과 odom, 상태 메시지와 TF의 역할을 구분한다.
- 한 페이지에 하나의 핵심을 두고 수식·계산 결과·도식·짧은 항목으로 구성한다. 차동 구동의 궤적과 yaw–quaternion 관계는 슬라이더로 조작할 수 있다.
- 새 스타일과 시연은 `mobile-chapter.css`, `mobile-model.js`, `mobile-demo.js`로 분리한다. 본문 모델의 계산 검증은 `tools/check-mobile-model.cjs`, 출처와 설명 기준은 `MOBILE_ROBOT.md`에 정리한다.
- 챕터 표지는 2·11·24·45페이지다. 강의 ②는 49장, 전체 강의는 131장, OT 포함 총 143장이다.

# v31 — 강의 ② M1 팔 매니퓰레이션 중심의 챕터 4
2026-09-08. 챕터 4를 7자유도 팔로 물체에 접근하고 잡고 옮기는 본문 18장으로 확장했다.

- 46~55페이지는 M1 관절 구조·관절/작업 공간·TCP·순기구학·역기구학·여유자유도를 설명한다. 52·54페이지에는 실제 URDF의 팔 연결에 기반한 FK·IK 시연을 넣는다.
- 56~63페이지는 작업공간·특이점·충돌·집기 단계·경로·궤적·피드백·카메라 좌표를 하나의 작업 흐름으로 연결한다.
- 기존 M1 사진을 관절 주석, 팔 자세 비교, 파지 중심, 접근·파지·들어 올리기, 카메라 설명에 사용한다. 사진은 틴트 패널 위에 두고 모델 도식과 역할을 구분한다.
- 기구학 모델·TCP 가정·참고 자료와 검증을 `ARM_MANIPULATION.md`에 정리한다. `arm-chapter.css`, `arm-model.js`, `arm-demo.js`를 추가하며 앞 챕터의 수식·카드 구성요소를 재사용한다.
- 표지와 챕터 목차의 04 항목은 매니퓰레이션 / 순기구학·역기구학으로 맞춘다. 챕터 표지 위치는 유지하며 강의 ②는 63장, 전체 강의는 145장, OT 포함 총 157장이다.

# v32 — 챕터 제목의 제어 용어 통일
2026-09-08. 강의 ② 표지와 챕터 목차의 04 제목을 ‘팔 제어’로 맞췄다. ‘모터 제어·오차 제어·이동 제어·팔 제어’의 병렬 구조를 유지하며, 본문의 매니퓰레이션 내용과 순기구학·역기구학 부제는 유지한다.

# v33 — 강의 ② 관절 한계의 범위와 제한 동작
2026-09-09. 23페이지를 ‘관절 한계: 허용 범위 안에서 움직이기’로 보강했다.

- M1 상체 사진과 각도 축을 나란히 두고, 소프트 한계와 하드웨어 한계를 색과 이름으로 구분한다. 현재각·요청 목표·제한한 목표를 서로 다른 표식으로 나타낸다.
- 하단에 목표 제한, 미리 감속, 실제 상태 확인과 출력 제한을 배치해 PID와 관절 한계의 관계를 연결한다.
- 사진의 근거 없는 어깨 허용각 곡선과 ‘한계를 넘지 않는다’는 단정적 표현을 없앤다. 도식은 실제 M1 허용각이 아닌 개념 예시로 표시한다.
- 스타일은 `.lecture-2 .joint-limit-*`에 한정한다. 작은 화면에서는 세로로 읽고 각도 축은 옆으로 밀어 볼 수 있다. 강의 ② 63장과 챕터 순서는 유지한다.
- 설명의 범위와 공식 참고 자료는 `PID.md`의 23페이지 항목에 정리한다.

# v34 — 강의 ② 시뮬레이션 배경에서 P·D 계산 방식으로 연결
2026-09-09. 21~22페이지를 시뮬레이션을 처음 접하는 수강생 기준으로 재구성했다.

- 21페이지는 물리 시뮬레이션의 정의를 먼저 제시하고, 현재 20°인 가상 관절을 목표 30°에서 멈추는 그림과 P·D 식으로 Stiffness·Damping을 설명한다. 정지 목표의 속도 0을 명시한다.
- 22페이지는 가상 시간 0.00 → 0.01 → 0.02 s의 상태 갱신으로 한 스텝을 설명한 뒤, 토크를 먼저 정하는 방식과 끝 상태·토크를 함께 구하는 방식을 비교한다. Explicit·Implicit은 보조 이름으로 표시한다.
- 솔버·제약·Force drive를 배경 설명 없이 앞세우지 않는다. 상세한 구현 범위와 단위, 중력 보상, 계산 방식별 조건은 `PID.md`에 유지한다.
- 스타일은 `simulation-basics.css`의 `.lecture-2 .sim-*`에 한정한다. 모바일에서는 시간 흐름과 계산 순서를 세로로 읽도록 배치한다. 기존 23페이지 보강과 전체 63장 구성을 유지한다.

# v35 — 강의 ② 22페이지 도식과 설명의 겹침 수정
2026-09-09. 1536×864·1440×900·1920×900 화면에서 도식 아래 설명이 겹치는 현상을 재현하고 배치를 수정했다.

- 도식 행이 내용보다 작아지지 않도록 최소 높이를 보존하고, 하단 설명을 구분선과 여백이 있는 별도 행으로 둔다.
- 화면 높이 850px 전후에서 글자·여백 크기가 급변하던 배치 대신 화면 크기에 따라 연속적으로 조정한다. 넓은 화면에서는 비교 제목과 보조 용어를 나란히 놓는다.
- 폭 1100px 이하에서는 두 비교 카드를 세로로 배치한다. 필요한 경우 슬라이드 높이가 내용에 맞춰 늘어나도록 해 설명을 겹치거나 잘라 표시하지 않는다.
- 변경은 `.sim-step-slide`에 한정하며 CSS 주소의 버전을 갱신한다. 21페이지와 강의 내용·페이지 번호를 유지한다.

# v36 — 강의 ② 25페이지 예제 로봇 크기 명시
2026-09-09. 25페이지의 ‘계산용 치수’를 ‘예제에서 가정한 로봇 크기’로 바꾸고 수치·측정 위치·계산 용도를 연결했다.

- 바퀴 반지름 r = 10 cm = 0.10 m, 바퀴 중심 간격 L = 50 cm = 0.50 m를 각각 구분해 적는다. 이후 계산과 주행 시연에 공통으로 사용하는 가정임을 밝힌다.
- 위에서 본 로봇에는 두 바퀴의 중심과 중심 사이 치수선을, 옆에서 본 바퀴에는 중심부터 테두리까지의 반지름을 표시한다.
- 각 값 옆에 바퀴 회전량 → 이동 거리, 좌우 이동 차이 → 로봇 회전각이라는 용도를 붙이고, 실제 M1 사양과 구분되는 강의용 가상 로봇임을 그림 아래에 명시한다.
- 변경은 25페이지와 `.mobile-dimensions-slide`, `.mobile-dimension-*`에 한정한다. 기존 r·L 값과 기구학 모델, 이후 페이지 계산은 유지한다.

# v37 — 강의 ② 25페이지 단위와 설명 간소화
2026-09-09. 25페이지는 m 단위만 사용하고, r = 0.10 m와 L = 0.50 m를 왼쪽 표에 한 번씩 표시한다. 그림에는 두 길이의 측정 위치와 짧은 이름만 남긴다. 단위 환산, 수치 반복, 개별 계산 용도 문장을 덜어내고 예제용 가정임을 한 줄로 명시한다.

# v38 — 강의 ② 25페이지에 실제 M1 이동부 사진 활용
2026-09-10. 기존 M1 사선 사진을 이동부 중심으로 확대해 25페이지에 넣었다. 사진 옆에는 위에서 본 차동 구동 모델과 반지름 도식을 작게 배치해 실제 로봇과 계산할 두 길이를 연결한다. 가려진 바퀴 중심이나 실제 치수를 사진에 임의로 표시하지 않으며, m 단위·수치 1회 표기와 예제용 가정값 안내를 유지한다.

# v39 — 강의 ② 26페이지 계산 예시와 속도 환산 설명 정리
2026-09-10. 좌우 바퀴 계산을 각속도와 곱셈 과정이 보이는 두 줄로 나누고 각 결과에 m/s를 표시했다. rpm은 분당 회전수, 감속비는 모터 각속도를 바퀴 각속도로 바꿀 때 나누는 값으로 설명하며 10:1 예시를 붙였다. 그림 아래 문장과 핵심 문장을 각속도에서 선속도로 바꾸는 내용에 맞추고, 26페이지에만 적용되는 배치로 줄바꿈과 여백을 조정했다.

# v40 — 강의 ② 27페이지 움직임과 기호의 의미 보강
2026-09-10. 직진·왼쪽 선회·제자리 회전에 짧은 설명을 붙이고, 바퀴 속도 화살표와 로봇 이동·회전 경로를 서로 다른 색으로 표시했다. 바퀴 조건과 로봇 결과를 두 행으로 나눴으며, 하단에 vL·vR·v·ω의 대상을 명시해 앞 페이지의 바퀴 각속도와 구분한다. 전진·반시계 회전 예시에 맞게 부호 조건을 적고 27페이지 전용 스타일로 화면 크기별 배치를 조정했다.

# v41 — 강의 ② 28페이지 계산 조건과 결과 해석 정리
2026-09-10. 좌우 바퀴 속도와 중심 간격 L을 상단에 표시하고, ‘성분’이라는 설명을 로봇 중심의 이동 속도·몸체 방향이 바뀌는 속도로 풀어 썼다. 두 계산 결과를 이동하면서 왼쪽으로 회전한다는 한 문장으로 연결했다. 기존 수식과 간격에 따른 회전 속도 관계를 유지하며 28페이지에만 적용되는 배치로 정리했다.

# v42 — 강의 ② 28페이지 정기구학 개념 한 문장 추가
2026-09-10. 제목 아래에 바퀴 속도를 알 때 로봇의 이동·회전 속도를 구하는 것이 정기구학이라는 설명을 붙였다. 정의에서 예제 조건과 수식으로 이어지도록 하고 기존 계산·페이지 순서를 유지한다.

# v43 — 강의 ② 29페이지 ICC 정의와 회전 반경 그림 보강
2026-09-10. ICC의 정의와 R이 나타내는 거리를 먼저 설명하고, 안쪽 바퀴·로봇 중심·바깥쪽 바퀴의 동심 원호와 치수선을 연결했다. 세 반경을 R − L/2, R, R + L/2로 표시하며 같은 시간에 바깥쪽 바퀴가 더 멀리 가므로 빨라야 한다는 문장으로 정리한다. 바퀴 속도 식은 반경과 곱하는 형태로 줄이고, 직진·제자리 회전의 조건에서 완전 정지를 구분했다. 29페이지 전용 스타일과 작은 화면의 가로 스크롤을 적용한다.

# v44 — 강의 ② 29페이지 ICC 약자 풀이와 로봇 몸체 연결 복원
2026-09-10. 페이지 상단에 ICC · Instantaneous Center of Curvature를 표시했다. 도식의 몸체 폭을 양쪽 바퀴 안쪽까지 넓혀 바퀴가 떨어져 보이지 않도록 했다. 바퀴·로봇 중심과 세 회전 반경, 계산값을 유지한다.

# v45 — 강의 ② 29페이지 ICC·R의 활용 목적과 역기구학 연결
2026-09-10. 제목을 ‘ICC와 R: 원하는 곡선으로 주행하기’로 바꾸고 현재 회전 반경 해석(R = v/ω)과 원하는 곡선의 회전 속도 결정(ω = v/R)을 나란히 설명한다. 반경 1.25 m로 왼쪽 선회하는 목표에서 ω = 0.4 rad/s를 구하고, 원하는 반경 → 로봇 속도 → 다음 페이지의 바퀴 명령으로 이어지도록 했다. ICC 영문 풀이와 연결된 몸체·바퀴 그림, 세 반경 및 바퀴 속도 식, 직진·제자리 회전 조건을 유지했다.

# v46 — 강의 ② 30페이지 역기구학 계산과 속도 명령의 대상 명확화
2026-09-10. 29페이지에서 정한 로봇 속도를 입력으로 받아 바퀴 각속도 명령을 구한다는 정의를 추가했다. 좌우 선속도를 먼저 구하고 바퀴 반지름으로 나누는 두 단계로 계산을 나눠, 같은 숫자 0.10의 서로 다른 단위를 구분한다. `/cmd_vel`의 linear.x·angular.z가 로봇 몸체 속도에 대응한다는 예시를 입력 쪽에 배치하고, 출력 쪽에는 왼쪽 4 rad/s·오른쪽 6 rad/s를 표시했다. 변경 스타일은 30페이지에 한정한다.

# v47 — 강의 ② 31페이지 좌표 기준과 위치 추정 목적 보강
2026-09-11. 제목을 ‘로봇의 속도로 위치 변화를 계산한다’로 바꾸고, 세 수식을 x·y 방향 속도와 방향 변화 속도로 설명한다. 위치·방향 기호와 기호 위 점의 의미, 바닥 +x축을 기준으로 한 θ의 정의를 표시했다. 그림과 계산은 현재 45° 방향으로 0.5 m/s 전진하는 순간으로 통일하고 각 성분을 약 0.35 m/s로 계산한다. 몸체 전방과 바닥 좌표의 차이를 쉬운 문장으로 풀고, 짧은 시간의 이동량 및 오도메트리로 이어지는 목적을 하단에 덧붙였다. 31페이지 전용 배치와 작은 화면의 그림 가로 스크롤을 적용한다.

# v48 — 강의 ② 32페이지 엔코더 측정부터 이동·회전량까지 계산 연결
2026-09-11. Δ의 의미와 좌우 바퀴를 같은 측정 구간에서 비교한다는 조건을 먼저 설명했다. 공통 예제 크기를 상단에 두고, 왼쪽에는 회전량에 반지름을 곱해 0.04 m·0.06 m를 얻는 과정을 표시했다. 나란한 두 결과 카드에서는 평균으로 중심 이동량, 차이를 간격으로 나눠 방향 변화량을 구한다. 중심 이동량은 경로를 따라 간 길이이고 Δθ는 몸체 회전량임을 구분하며, 다음 페이지의 위치·방향 누적으로 연결한다. 32페이지 전용 배치로 데스크톱은 세 칸, 작은 화면은 세로 순서로 읽도록 했다.

# v49 — 강의 ② 33페이지 중간 방향과 위치·방향 누적 과정 보강
2026-09-15. 중간 방향을 구하는 단계와 이동량을 x·y로 나눠 현재 값에 더하는 단계를 분리했다. 현재·다음 표기로 갱신 전후를 구분하고, 위치 식에 근사 기호를 사용한다. 시작·중간·끝 접선 방향을 보여주는 개념도를 새로 그렸으며, 회전 차이를 크게 그린 그림과 작은 Δθ에서 사용하는 근사라는 점을 표시했다. 32페이지 예제에서 중간 방향 0.02 rad를 구하는 과정과 x·y 계산을 한 줄씩 연결한다. 시연 구현에 관한 문장 대신 누적(적분)의 의미를 설명하고 33페이지 전용 반응형 배치를 적용했다.

# v50 — 강의 ② 34페이지 주행 비교와 시간 탐색 보강
2026-09-15. 바퀴 각속도와 전진·후진 부호를 설명하고 오른쪽 선회 예시를 추가했다. 0~6초 시간 슬라이더로 장면을 직접 확인하며, 재생·일시정지·완료 상태를 안내한다. x·y·θ를 m·rad의 소수 셋째 자리로 표시해 앞 페이지의 짧은 이동과 연결한다. 반경은 절댓값으로 읽고 정지·직진·후진·선회·제자리 회전에 맞는 설명을 보여준다. 4·6과 2·3 rad/s의 비교 과제, 격자 단위와 자동 확대, 이상적인 예측 경로 안내를 추가했다. 몸체와 바퀴가 연결되게 도형을 조정하고 34페이지 전용 배치를 적용했다.

# v51 — 강의 ② 실습 64·65를 요구사항만 남겨 간소화
2026-09-17. 사용자 직접 지시. "감쇠 없이는 2850 rad를 요구한다"처럼 구현 세부와 기호가 섞여 한눈에 읽히지 않았다. 설명을 덧붙인 판은 너무 자세하다는 의견에 따라 각 실습을 한 줄 요구사항 4개로 줄인다.

- 단계에는 무엇이 되어야 하는지와 통과 기준만 적는다: q=0 손끝 (0, 0.24, −0.592) m, 로봇 `/tf`와 0.1 mm 이내, FK로 만든 목표를 IK로 되풀었을 때 위치 오차 0.01 mm 미만.
- 설치 명령·API 호출·QoS·메시지 형식 같은 구현 방법은 슬라이드에서 빼고 `ARM_MANIPULATION.md`의 실습 절에 둔다. 팔 명령 규약과 `oMf` 기준 프레임 주의는 이번에 그쪽으로 옮겼다.
- 원래 6단계는 1536×864 이하에서 겹쳤다. 한 줄 4단계로 줄이자 CSS를 바꾸지 않고도 1280×720~1920×1080의 10개 크기에서 겹치지 않는다.

# v11 — 강의 ③ 실습 슬라이드 복원
2026-09-08. 사용자 직접 지시. v8의 "실습 12장 삭제"를 강의 ③에 한해 뒤집는다.

- 강의 ③ 끝에 실습 6장(48~53)을 붙인다: 센서 켜기·camera_info / TF 트리·optical frame /
  오도메트리 드리프트 / 라이다 SLAM·맵 저장 / Nav2 주행·포즈 깨뜨리기 / 스테레오 disparity.
- v8이 실습을 지운 이유는 "작성 예정"이 반복돼 미완성으로 보였기 때문이다(§1). 이번에는
  빈칸 대신 **실제 명령과 확인 포인트를 채운다** — 채울 자리가 아니라 따라 할 절차다.
- 레이아웃은 §5의 워크시트 규약을 그대로 쓰되 `layout-practice-b`(좌: 목표·확인 / 우: 단계
  목록)를 적용한다. CSS에만 있고 미사용이던 `practice-*` 클래스를 처음 쓴다.
- 단계 항목은 알약(999px)에서 카드(18px)로 바꾸고 내용에 따라 높이가 늘도록 한다. 원래
  컴포넌트는 한 줄 빈칸용이라 명령어가 들어간 문장을 받지 못했다.
- 명령어는 `<code>`로 감싸 악센트 틴트 배경을 준다.
- 실습 100분을 덱 밖에서 운영한다는 v8 규칙은 그대로다. 이 6장은 그 시간에 띄워 두는
  절차 참조용이다.
- 강의 ③은 53장, 전체 강의는 104장이다.

# v12 — 강의 ③ 31p 루프 클로저 색 범례
2026-09-14. 사용자 직접 지시.

- 31p 매핑 영상은 RTAB-Map의 Loop closure detection 창이고, 그 안의 특징점 색이
  이 강의에서 설명한 루프 클로저 판정 결과다. 영상만 틀면 색의 뜻을 알 수 없으므로
  오른쪽에 범례를 둔다: 빨강(기하 불일치·거부) / 노랑(지역 루프 클로징) /
  초록(전역 루프 클로징).
- 범례 색은 영상에서 쓰는 색을 그대로 쓴다. §6의 악센트 규칙(강조는 강의 악센트색)보다
  **영상과의 대조 가능성**이 우선한다 — 범례가 영상과 다른 색이면 아무 의미가 없다.
- 핵심 한 줄은 오른쪽 카드에서 슬라이드 하단 전체 폭으로 내린다. 범례가 그 자리를 쓴다.
- 새 컴포넌트: `.legend-card` + `.color-legend`(색 점 · 라벨 · 설명).
- 같은 날 추가했던 구현 사례 12장(v12 초안)은 사용자 요청으로 되돌렸다. 강의 ③은
  다시 이론 47 + 실습 6 = 53장이다.

# v13 — 강의 ③ 입문자용 설명 보강 8장
2026-09-14. 사용자 직접 지시. "처음 VSLAM 하는 사람도 이해되게" 용어를 풀어 쓴다.

- 다이어그램만 있고 설명 자리가 없는 슬라이드가 많다. 그림을 줄여 불릿을 끼우는 대신
  **바로 뒤에 설명 한 장을 붙인다.** 그림은 그대로 두고, 용어는 표로 받는다.
- 8장을 넣는다: 11 절대 스케일(VIO 정의) / 14 뎁스를 만드는 방식 / 19 프론트엔드와 백엔드 /
  28 같은 장소를 찾는 순서(지역·전역 루프) / 35 지도 표현 세 가지 / 39 코스트맵의 네 겹 /
  43 A*는 무엇을 비교하는가(f = g + h) / 51 무엇이 어떤 토픽으로 오가는가.
- 설명 장은 `concept-table`(용어 | 설명)을 기본으로 쓴다. 입문자에게는 이 형식이 가장 빨리
  읽힌다. 대립이 아니라 역할 분담인 프론트·백엔드는 `comparison-grid`, 계산 규칙이 그림으로
  설명되는 A*는 `layout-split` + 격자 그림을 쓴다.
- 기존 14장은 제자리에서 손본다: 12p 시차·기선을 먼저 정의, 13p 핵심 한 줄을 RGB-D 정의로,
  18p 핵심 한 줄을 프론트·백엔드 요약으로, 20p K 안의 fₓ·cₓ를 풀어 쓰고, 24p 삼각측량을
  정의 문장으로, 26p 루프 클로저를 정의 문장으로, 42p A* 한 줄을 비용 비교로,
  45p 지역 제어기 표에 공통 역할 행 추가.
- `concept-table` 안의 `<code>`에 실습 슬라이드와 같은 틴트를 준다(토픽 이름용).
- 슬라이드 번호가 밀리므로 슬라이드별 CSS(`#slide-N`)와 본문의 "슬라이드 N" 참조를 함께
  옮긴다. 실습 5의 진단 순서 참조는 55p, 실습 6의 오차식 참조는 12p다.
- 43p는 "왜 A*인가"와 "비용을 어떻게 더하는가"를 한 장에 담으려다 둘 다 얕아져, 이름·평가
  함수(43p)와 실제 계산(44p)으로 나눴다. 44p는 세 후보의 g·h·f를 숫자로 나란히 놓고
  칸 비용이 붙은 후보가 어떻게 밀리는지 보여 준다.
- 강의 ③은 62장(이론 56 + 실습 6), 전체 강의는 152장이다.

# v14 — 강의 ③ 용어 보강과 A* 탐색 절차 1장
2026-09-15. 사용자 직접 지시.

- 4p 비교 카드 제목을 `VSLAM` · `Navigation`으로 줄인다. "주는 것 / 더 필요한 것"은
  카드 안의 항목이 이미 말하고 있어 제목에서 겹쳤다.
- 20p 투영 모델이 맥락 없이 등장한다는 지적. 첫 불릿을 다리 문장으로 바꾼다 —
  19p의 프론트·백엔드 둘 다 "3차원 점이 어느 픽셀에 맺히는가"를 전제로 계산되므로
  그 규칙을 여기서 정의한다. 불릿 4개 상한을 지키려고 기존 3·4번을 한 줄로 합쳤다.
- BA를 이름 없이 쓰고 있었다. 25p는 이미 BA를 그림과 식으로 설명하면서 이름만 빠져
  있어 그 자리에서 `번들 조정(BA)`으로 명명하고, 29p의 `전역 BA`는 "지도 전체의
  키프레임과 지도점을 한꺼번에 다시 조정"으로 뜻을 붙였다. 지역/전역 구분은 28p가 이미 한다.
- A*는 f = g + h(43p)와 계산(44p)까지만 있고 **탐색이 어떻게 진행되는지**가 없었다.
  45p `A*가 도는 순서`를 `concept-table`로 추가한다: 열린 목록 / 닫힌 목록 / 이웃 갱신 /
  경로 복원. 부모 포인터와 경로 복원이 빠지면 학생이 "답이 어떻게 나오는지"를 못 본다.
- 슬라이드가 밀리므로 45p 이후의 id·aria-label을 다시 매기고, `deck.css`의
  `#slide-46·49·55·56`을 `#slide-47·50·56·57`로, 실습 5의 "슬라이드 56" 참조를 57로 옮겼다.
- 강의 ③은 63장(이론 57 + 실습 6), 전체 강의는 153장이다.

# v51 — 강의 ② 35페이지 오도메트리의 계산 흐름과 드리프트 보강
2026-09-16. 사용자 직접 지시.

- 엔코더 회전량 측정 → Δs·Δθ 계산 → x·y·θ 누적을 세 단계로 나눠 32~34페이지와 연결한다.
- 센서가 실제 몸체 이동을 그대로 확인한다는 인상을 없애고, 헛도는 바퀴의 예로 추정 오차를 설명한다.
- 폐곡선 그림의 공통 출발점·실제 도착·추정 도착과 누적 오차를 구분하고, 실선·점선 범례를 추가한다.
- 드리프트의 의미와 짧은 구간 추적에 유용하다는 점을 함께 적고 `odom` 좌표계 소개로 이어 간다.
- 데스크톱에서는 두 열, 좁은 화면에서는 세로 배치와 키보드로 조작 가능한 그림 가로 스크롤을 사용한다.

# v52 — 강의 ② 36페이지 좌표계 정의와 같은 점의 좌표 비교
2026-09-16. 사용자 직접 지시.

- 원점·좌표축을 ‘어디가 0인가 / 어느 방향이 +인가’로 설명하고, 세 프레임이 붙는 대상을 명시한다.
- odom의 (2, 1)에서 반시계 90°를 향한 로봇과 전방 1 m의 점 P로 기존 개념도를 구체화한다.
- P의 몸체 기준 (1, 0)과 바닥 기준 (2, 2)를 나란히 보여 주고, 같은 점의 다른 표현임을 강조한다.
- 몸체의 x·y·z 방향을 위에서 본 그림과 연결하며, 원점·축·숫자 눈금과 바퀴 연결을 맞춘다.

# v53 — 강의 ② 36페이지 그림에 라이다 좌표계 추가
2026-09-16. 사용자 직접 지시.

- 몸체 전방 0.3 m에 라이다를 장착한 예로 `laser_link` 원점과 x·y 축을 주황색으로 표시한다.
- 몸체 축과 평행하다는 가정을 그림에 명시하고, 센서 앞 0.7 m의 같은 점 P를 세 프레임으로 비교한다.
- 세 좌표 카드를 나란히 놓으며 모바일에서는 세로로 배치한다. 센서 위치는 학습용 예시다.

# v54 — 강의 ② 35~36페이지 오도메트리와 odom 용어 비교
2026-09-16. 사용자 직접 지시.

- 35페이지 하단에서 오도메트리는 ‘추정 과정’, odom(오돔)은 ‘좌표계 이름’으로 나란히 비교한다.
- 36페이지 odom 행과 그림의 도입에서 추정한 로봇 자세를 해당 기준으로 읽는 관계를 다시 설명한다.
- 센서·몸체·공간 기준의 세 좌표 비교와 기존 드리프트 설명은 유지한다.

# v55 — 강의 ② 37페이지 프레임 역할과 위치 보정 예시 보강
2026-09-16. 사용자 직접 지시.

- 추상적인 장기·지역 기준과 pose 표현을 풀어 쓰고, 각 좌표계의 기준·특성·활용을 정리한다.
- 지도 보정의 주체를 위치추정기로 명시하고, 정지 중 map·odom 좌표가 어떻게 다른지 숫자로 비교한다.
- map의 2.4→2.0 m 보정과 odom의 2.4→2.4 m 유지를 구분해 두 프레임을 함께 쓰는 이유를 강조한다.
- base_link의 설계상 원점과 로봇 기준 장애물 표현을 설명한다. 좁은 화면에서는 카드를 세로로 읽는다.

# v56 — 강의 ② 37페이지 지도 보정 전후의 예시 그림 추가
2026-09-16. 사용자 직접 지시.

- 숫자 예시를 두 장의 지도 그림으로 바꾼다. 실제 로봇·벽·몸체 좌표축은 같은 곳에 두고 주황색 추정 표시만 이동한다.
- 벽 위치 3 m와 거리 관측 1 m로 추정 위치를 2.4→2.0 m로 보정하는 단순화한 예를 보여 준다.
- 그림 아래 odom 값은 전후 2.4 m를 유지해 물리적 이동·지도 기준 보정·연속적인 이동 추정을 구분한다.
- 설명 카드를 줄여 그림 공간을 확보하고, 모바일에서는 도식만 가로로 스크롤한다.

# v57 — 강의 ② 37페이지 지도 보정의 프레임 관계 시각화
2026-09-17. 사용자 제공 개념 그림 반영.

- 오도메트리, 지도 위치추정, map→odom 보정을 ①②③으로 연결하고 같은 번호·색상을 그림에 사용한다.
- 같은 odom·base_link의 보정 전후 추정 위치를 겹쳐 그린다. 두 위치를 같은 양만큼 옮겨 odom→base_link 관계가 유지됨을 보여 준다.
- 실제 로봇은 정지한 상황이며 평행이동 보정만 표현한다는 조건을 명시한다.
- 지도 위치추정 결과인 map→base_link는 점선으로 구분하고, 실제 TF 연결은 map→odom→base_link로 별도 표시한다.
- 데스크톱에서는 관계 그림과 설명 카드를 나란히, 모바일에서는 카드를 세로로 배치하고 그림만 가로로 스크롤한다.

# v58 — 강의 ② 37페이지 지도 보정의 활용 의미 보강
2026-09-17. 사용자 직접 지시.

- 지도 기준 로봇 위치가 2.4→2.0 m로 보정되면, x=5 m의 목표까지 남은 거리가 2.6→3.0 m로 바뀌는 1차원 예를 추가한다.
- 기존 세 관계 설명을 짧게 정리하고, 숫자 비교를 별도 테두리 상자에 두어 관계 그림과 함께 읽을 수 있게 한다.
- 하단 문구는 odom의 연속성을 유지하면서 보정된 지도 위치로 목표까지 거리·방향을 계산한다는 활용 의미를 설명한다.

# v59 — 강의 ② 39페이지 좌표변환 예제를 처음 읽는 순서로 재구성
2026-09-18. 사용자 직접 지시.

- 몸체 위치·방향, 센서 장착 위치, 센서가 관측한 점을 먼저 제시한 뒤 세 단계 계산을 설명한다.
- 몸체 앞 1.3 m가 odom의 +y 방향 1.3 m라는 의미를 풀어 쓰고, 중간값 (0, 1.3)은 몸체에서 점까지의 변위로 명시한다.
- 그림에 로봇·base_link·laser_link·점 P와 몸체 축, odom 눈금, 0.3 m·1 m 거리 표시를 추가한다.
- 일반식의 R(θ)와 t에 설명을 붙이고, 같은 점을 다른 기준으로 읽는 좌표변환임을 강조한다.
- 조건과 계산·그림을 데스크톱 한 화면에서 읽도록 배치하고, 모바일에서는 그림만 가로로 스크롤한다.

# v60 — 강의 ② 39페이지 회전행렬 대입과 곱셈 풀이 추가
2026-09-18. 사용자 직접 지시.

- 회전행렬의 cos·sin 일반식에 90°를 대입해 숫자 행렬을 얻는 과정을 표시한다.
- 숫자 행렬과 몸체 좌표 열벡터를 곱하고, 각 행의 곱셈·덧셈을 전개해 변위 (0, 1.3) m가 나오는 과정을 보여 준다.
- 뒤이어 몸체 위치 (2, 1)을 더하는 세 번째 단계와 연결하고, 일반식과 물리적 의미는 하단에 요약한다.
- MathML의 행렬 괄호·열벡터와 수학 서체를 사용하며, 모바일에서는 수식 영역을 따로 가로 스크롤할 수 있다.

# v61 — 강의 ② 42페이지 Odometry 필드와 좌표계의 의미 구체화
2026-09-18. 사용자 직접 지시.

- 필드 나열 표를 위치·방향과 속도 두 상자로 바꾸고, 각 frame_id 설정과 숫자의 해석을 함께 보여 준다.
- 위치 (2, 1, 0)과 quaternion을 odom의 원점·축 기준으로 풀어 쓰고, linear.x와 angular.z는 몸체의 이동·회전 속도로 설명한다.
- yaw 90°로 전진하는 예에서 몸체 전방 0.5 m/s가 odom의 +y 방향 0.5 m/s임을 연결해 ‘어느 축으로 읽는가’를 구체화한다.
- 추정 시각과 불확실성은 보조 정보로 정리하고, 데스크톱의 두 열과 모바일의 세로 배치에서 가독성을 확인한다.

# v62 — 강의 ② Odometry 필드 예시 페이지 삭제
2026-09-18. 사용자 직접 지시.

- 기존 42페이지의 Odometry 필드 예시와 전용 스타일을 삭제한다. quaternion 시연 뒤에는 /odom과 TF의 관계 설명이 이어진다.
- 뒤쪽 슬라이드를 한 장씩 당겨 총 62장으로 맞춘다. 챕터 4 표지는 44페이지, FK·IK 시연은 각각 51·53페이지가 된다.
- 본문의 시연 참조, 접근성 페이지 번호, 첫 화면의 장수와 강의 구성 문서를 함께 갱신한다.

# v63 — 강의 ② 43페이지 명령과 측정의 흐름 구체화
2026-09-18. 사용자 직접 지시.

- 한 장의 꺾인 흐름도를 명령·구동과 측정·추정의 두 행으로 나누고, 각 단계를 짧은 설명과 숫자로 연결한다.
- 직진 명령 0.5 m/s에서 바퀴 목표 5 rad/s를 구하고, 측정 예시 4.8 rad/s에서 0.48 m/s·0.048 m를 추정하는 과정을 보여 준다.
- 엔코더의 0.1초 평균 측정과 미끄러짐이 없다는 가정을 명시하고, 측정값이 PID와 오도메트리에 함께 쓰인다는 점을 강조한다.
- 모바일에서는 단계와 화살표가 세로로 이어진다. 44페이지의 팔 제어 챕터 표지와 전체 62장 구성은 유지한다.

# v64 — 강의 ② 22페이지 명시적·암시적 구동의 응답 특성 보강
2026-09-18. 사용자 직접 지시.

- 기존 시간 흐름과 두 계산 순서를 유지하고 각 카드에 장점과 게인·Δt에 따른 응답 특성을 추가한다.
- 명시적 방식의 단순한 토크 계산과 진동·발산 가능성, 암시적 방식의 큰 게인 처리와 수치 감쇠를 짧게 비교한다.
- 작은 스텝·낮은 게인에서의 유사성, 접촉·관절 한계가 있을 때 암시적 방식의 한계를 명시한다. Δt를 줄여 안정성과 정확도를 함께 살피도록 안내한다.
- 22페이지에 한정해 설명 행과 카드 간격을 조정하며, 다른 페이지와 전체 62장 구성은 유지한다.

# v65 — 강의 ② PID 실험 뒤에 게인 스케줄링 소개 추가
2026-09-18. 사용자 직접 지시.

- 20페이지와 기존 21페이지 사이에 게인 스케줄링 소개를 넣고, 조건 확인 → 게인 선택·보간 → PID 계산을 구분한다.
- 물체 무게에 따른 게인 세트 A·B와 중간 값의 보간을 예로 들며, 게인을 미리 튜닝한다는 점과 부드러운 전환·적분 상태 관리의 필요성을 설명한다.
- 새로운 21페이지를 추가해 총 63장으로 맞춘다. 시뮬레이션 설명은 22~23페이지, 관절 한계는 24페이지, 이동 제어·팔 제어 표지는 각각 25·45페이지가 된다.
- 본문·구성 문서·첫 화면의 페이지 수와 참조를 갱신한다. 원본 PowerPoint의 페이지 번호와 과거 변경 기록은 유지한다.

# v66 — 강의 ② 49페이지 TCP를 작업점·방향·기준 좌표계 순서로 설명
2026-09-18. 사용자 직접 지시.

- TCP의 약자와 작업 기준점이라는 뜻을 먼저 제시하고, 집기·펜 쓰기의 예로 어느 점을 움직이는지 설명한다.
- 좌표축으로 표현하는 방향, 위치와 방향을 합친 pose, 몸통 기준 좌표계를 세 단계로 정리한다.
- M1 손 사진에 손목 기준점과 TCP를 구분하고, 두 점의 관계와 TCP 좌표축을 표시한다. 그림의 점·축이 설명용임을 명시한다.
- 목표에 맞출 점이 TCP라는 의미를 하단에 강조한다. 데스크톱에서는 한 화면에 배치하고, 모바일에서는 그림을 가로로 스크롤할 수 있게 한다.

# v67 — 강의 ② 49페이지 엔드 이펙터와 TCP의 차이 보강
2026-09-18. 사용자 직접 지시.

- 제목과 상단 비교 문구에 엔드 이펙터는 손·도구라는 장치, TCP는 작업의 기준점이라는 차이를 명시한다.
- 본문을 말단 장치 → TCP의 예 → 위치·방향과 기준 좌표계 순서로 정리한다. TCP의 영문 약자 풀이와 손목에서 TCP까지의 관계는 유지한다.
- M1 사진에 손 전체를 가리키는 엔드 이펙터 표시를 추가하고, 청록색 TCP 점과 함께 읽도록 설명을 보완한다.
- 네 가지 화면 크기에서 글·그림의 배치, 모바일 그림 스크롤과 페이지 이동을 확인한다.

# v68 — 강의 ② 53페이지 방향 목표 설명을 자연스러운 순서로 정리
2026-09-18. 사용자 직접 지시.

- 위치만 지정하는 경우와 방향까지 지정하는 경우의 차이를 하단의 주 문장으로 설명한다.
- RPY = (0, 0, 0)은 TCP 축을 몸통 좌표계의 각 축과 같은 방향으로 맞추는 구체적인 예시로 분리한다.
- 사진 설명은 물체와 손의 방향이 맞지 않으면 잡기 어렵다는 의미로 다듬고, PC·태블릿·모바일에서 배치를 확인한다.

# v69 — 강의 ② 55페이지 자코비안의 개념과 수치 IK 연결 보강
2026-09-18. 사용자 직접 지시.

- 자코비안을 관절의 작은 변화가 TCP의 이동·회전에 미치는 변화율의 행렬로 먼저 정의한다.
- Δx ≈ J(q) Δq의 각 기호와 M1의 6×7 크기를 풀어 쓰고, 네 번째 열을 팔꿈치 q₄의 영향으로 읽는 예를 추가한다.
- 목표 오차 계산 → 관절 수정량 계산 → 갱신과 반복의 순서로 수치 IK에 연결한다. 작은 변화의 근사라는 점과 자세가 바뀔 때 J를 다시 계산하는 이유를 강조한다.
- 네이티브 HTML 수식 배치와 두 설명 영역을 사용하고, 네 가지 화면 크기에서 가독성과 페이지 이동을 확인한다.

# v70 — 강의 ② 57페이지 rank와 특이 자세의 의미 구체화
2026-09-18. 사용자 직접 지시.

- rank를 현재 자세에서 독립적으로 만들 수 있는 TCP 순간 운동의 수로 먼저 정의한다.
- 일반 자세의 rank 6과 그림 속 rank 5를 비교하고, 독립 운동이 하나 줄어도 관절은 여전히 7개임을 명시한다.
- 그림에 가능한 옆 방향 속도와 팔 길이 방향의 순간 속도 0을 표시한다. 자세를 바꾸면 가능한 운동도 달라진다는 설명을 하단에 둔다.
- 특이점 근처의 관절 속도 문제와 대응을 유지한다. 모델의 rank·제한 방향을 재검증하고, PC·태블릿·모바일의 글·그림 배치를 확인한다.

# v71 — 강의 ② 61페이지 궤적의 시간·속도·가속도 관계 구체화
2026-09-18. 사용자 직접 지시.

- 경로와 궤적의 차이를 먼저 설명하고, 시간별 목표각·속도·가속도의 의미와 7개 관절의 한계를 연결한다.
- 한 관절을 0°에서 60°로 움직이는 4초·2초 계획을 버튼으로 비교한다. 목표각·속도 그래프, 1초 때 목표각, 최대 속도·가속도를 함께 갱신한다.
- 같은 경로와 가감속 형태에서 시간만 절반으로 줄일 때 속도 2배·가속도 4배가 되는 관계를 설명한다. 제어주기마다 목표를 보내고 측정값으로 오차를 줄이는 관절 제어와 연결한다.
- 예시의 계산 모듈·시연을 분리하고 경계조건·도함수·적분·최대값을 검증한다. PC·태블릿·모바일에서 그래프와 조작 상태를 확인한다.

# v72 — 강의 ② 63~65페이지 복습·실습 참조의 정합성 보강
2026-09-18. 사용자 직접 지시.

- 63페이지의 복습을 52페이지 FK 시연, 54페이지 여유자유도, 59~61페이지 집기 단계·경로·궤적과 연결하고 64~65페이지 실습으로 안내한다.
- 64페이지는 51페이지의 좌표변환 연결을 구현하고 로봇 TF로 검산하도록 정리한다. 실습의 `left_base_link`와 52페이지의 교육용 TCP가 서로 다른 기준점·축임을 명시한다.
- 65페이지는 실습 1의 프레임 정의를 이어 쓰고, 55페이지의 자코비안·57페이지의 특이점·58페이지의 제약·61페이지의 궤적을 연결한다. 원래 관절각의 재현 대신 목표 위치·방향을 검산한다.
- 페이지 참조에 이동 링크를 적용한다. 실습 네 단계는 유지하고, 세 페이지에 한정한 배치 조정으로 PC·태블릿·모바일에서 읽을 수 있게 한다.

# v73 — 강의 ② 실습 앞에 환경 설치 안내 추가
2026-09-18. 사용자 직접 지시.

- 실습 1 앞(64페이지)에 실습 준비 슬라이드를 넣는다. 실습 환경은 `github.com/HERoEHS/aeirobot-deploy`의 릴리스 tarball과 `deploy_setup.sh`로 준비하고, 문서는 `docs.arobot4all.com`을 본다.
- 단계는 저장소 → 준비물(Ubuntu 24.04 · NVIDIA GPU · 컨테이너 이미지 · 라이선스) → 기본 패키지 → 릴리스 내려받기 → 설치 스크립트 → 라이선스 등록 6단계다. 명령 칩은 한 단계에 하나만 둔다.
- 레이아웃은 실습과 같은 `practice-slide layout-practice-stack arm-practice-slide`를 쓰되 뱃지만 `실습 준비`로 구분한다. 같은 형식이어야 실습 직전에 끝내 두는 절차로 읽힌다.
- 슬라이드 id·aria-label은 `deck.js`의 `#slide-N` 해시와 1:1이므로 실습 1·2를 65·66으로 민다. 63페이지 복습의 안내와 66페이지의 "64페이지의 FK" 같은 본문 참조 번호도 함께 옮긴다.
- 강의 ②는 66장, 4개 강의는 203장, OT를 포함한 전체는 214장이다.
