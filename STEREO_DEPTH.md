# 스테레오 깊이 · 강의 ③ 12~15페이지

2026-09-21. 그림의 기호를 이해한 뒤 깊이 식과 오차 식을 직접 유도하도록 구성했다.

## 기존 12페이지 검토

기존 도식의 좌표 비율 자체는 맞았다. 도면 좌표로 기선 240, 깊이 480, 영상면 거리 240에 대해
겹쳐 그린 두 투영점의 차이는 120으로, 닮음비 240/480 = 120/240을 만족했다.
다만 다음 설명이 빠져 있거나 오해를 만들었다.

- 렌즈 앞의 평면은 계산용 **가상 영상면**이다. 실제 센서가 그 위치에 놓인다는 뜻이 아니다.
- 그림의 물리적 길이 f와 픽셀 단위 fₓ는 구분해야 한다. 이상 핀홀 모델에서
  fₓ = f / pₓ이며, pₓ는 가로 픽셀 피치다. 정류·리사이즈된 영상에서는 그 영상에 맞는
  유효 fₓ와 주점을 써야 한다. ROS CameraInfo의 원영상 K와 정류 영상 P가 항상 같지는 않다.
- Z는 광축 방향의 깊이이며 카메라에서 점까지의 유클리드 거리가 아니다.
- d = uL − uR에는 좌우 주점의 가로 좌표가 같다는 조건이 필요하다. 정류만으로는 충분하지 않다.
- 이상 모델의 Z = fₓB/d는 등식이다. 실제 측정 오차는 추정값과 오차식으로 구별한다.
- 부호 있는 오차에는 음수가 필요하다. 크기만 다룰 때는 |δZ| ≈ Z²|δd|/(fₓB)라고 쓴다.
  깊이 2배에 오차 4배라는 말에는 **고정된 fₓ·B, 같은 작은 |δd|**라는 조건이 붙는다.

## 12페이지 그림에서 fₓ와 d를 찾는 법

- 왼쪽 공간 그림의 f(mm) 치수선은 광학 중심부터 가상 영상면까지다. 주황색 연결선을 따라
  `fₓ = f / pₓ (px)` 상자로 올라가면, 같은 초점거리를 픽셀 단위로 환산하는 관계가 보인다.
  mm 길이에 곧바로 px 라벨을 붙이지 않고 물리적 f와 픽셀 단위 fₓ를 구별했다.
- 두 광선과 영상면이 만나는 점을 각각 uL·uR로 표시했다. 오른쪽의 별도 영상 좌표 그림에서는
  각 영상의 왼쪽 원점 0과 공통 주점 cₓ를 나란히 맞춘다. 각 점에서 내려온 점선 사이의
  주황색 양방향 치수선이 `d = uL − uR`이다.
- **공간 그림의 서로 다른 영상면 위 두 점 사이 거리 자체는 d가 아니다.** 원점을 맞춘 뒤의
  가로 좌표 차이를 픽셀로 재야 한다. 두 그림 모두 왼쪽 점은 주점 오른쪽, 오른쪽 점은 주점
  왼쪽에 있으며 주점 기준 오프셋 비율도 같다(왼쪽 : 오른쪽 = 5 : −3, 축척은 생략).

## 깊이식 유도

왜곡이 보정되고 수평으로 정류된 두 핀홀 카메라를 생각한다. 좌우의 유효 fₓ와 cₓ가 같고,
왼쪽 중심 CL=(0,0), 오른쪽 중심 CR=(B,0), 물체점 P=(X,Z)라고 두며 Y축은 생략한다.
Z>0, B>0이다. u는 각 영상의 왼쪽에서 오른쪽으로 증가하는 픽셀 좌표다.

한 카메라의 큰 삼각형(X,Z)과 작은 삼각형(x,f)은 닮았다. 같은 단위끼리 비를 취하면
x/f = X/Z이다. 영상면 길이를 픽셀로 바꾸면 u−cₓ=x/pₓ, fₓ=f/pₓ이므로:

```text
uL − cₓ = fₓ X/Z
uR − cₓ = fₓ (X−B)/Z
d = uL−uR = fₓ[X−(X−B)]/Z = fₓB/Z
Z = fₓB/d
```

주점이 다르면 보정된 시차 d=(uL−cₓL)−(uR−cₓR)를 쓴다. fₓ도 다르거나 미정류 영상이라면
이 단순 차이식을 그대로 사용하지 않고, 정류하거나 보정된 투영행렬로 삼각측량한다.

14페이지는 fₓ=600 px, B=0.12 m, cₓ=640 px, X=0.10 m인 교육용 예제다.
Z=2 m이면 uL=670, uR=634, d=36 px, Z=600×0.12/36=2 m다.
슬라이더는 점의 Z를 바꾸어 가상의 영상 측정을 만든다. 실제 측정 순서는 영상 대응점 → d → Z다.
표시 좌표를 소수 둘째 자리로 반올림한 다음 시차를 계산해, 화면에 보이는 뺄셈과 결과가 일치하게 했다.
1·2·4 m 예제는 좌표와 시차가 모두 정확하게 표현된다.

## 오차식 유도와 검산

fₓB=A를 고정하고 참 시차 d, 측정 시차 d+δd라고 하자.

```text
Z = A/d
δZ = A/(d+δd) − A/d = −A δd / [d(d+δd)]       (정확한 차이)
∂Z/∂d = −A/d²
δZ ≈ −A δd/d²                                (|δd| ≪ d)
|δZ| ≈ Z² |δd| / A
```

15페이지의 A=72 px·m, |δd|=0.5 px 예제는 다음과 같다.

| Z | d | 1차 근사 깊이 오차 크기 |
|---|---|---|
| 1 m | 72 px | 0.006944… m = 0.69 cm |
| 2 m | 36 px | 0.027777… m = 2.78 cm |
| 4 m | 18 px | 0.111111… m = 11.11 cm |

2 m에서 δd=+0.5 px이면 계산 깊이 72/36.5=1.9726027… m이고 정확한 차이는
−2.739726… cm다. 미분으로 구한 −2.777777… cm와 가까우며 서로 같지는 않다.
이 모델은 올바른 대응점의 작은 시차 오차만 다룬다. 잘못된 대응점·가림·캘리브레이션 오차를
같은 일정한 픽셀 오차로 취급하지 않는다. 통계적 표준편차도 선형화 아래에서는 같은 민감도를 곱하지만,
슬라이드의 δ는 표준편차가 아닌 부호 있는 측정 오차다.

## 공식 출처

- [OpenCV · Camera Calibration and 3D Reconstruction](https://docs.opencv.org/4.13.0/d9/d0c/group__calib3d.html):
  픽셀 단위 초점거리, 투영식, 물리적 f의 픽셀 환산, stereoRectify의 주점 일치 조건.
- [ROS 2 Jazzy · DisparityImage 정의](https://github.com/ros2/common_interfaces/blob/jazzy/stereo_msgs/msg/DisparityImage.msg):
  주점 오프셋을 뺀 시차, Z=fT/d, 시차 분해능에 따른 깊이 분해능의 1차 근사.
- [ROS 2 Jazzy · CameraInfo 정의](https://github.com/ros2/common_interfaces/blob/jazzy/sensor_msgs/msg/CameraInfo.msg):
  optical frame 축, 원영상 K와 정류 영상 P의 구별, 오른쪽 P의 Tx=−fₓ′B.

유도와 수치 검산은 위 모델을 적용해 직접 계산했다.
